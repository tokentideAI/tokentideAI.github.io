[
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "About this site"
  },
  {
    "objectID": "posts/01_info_theory.html",
    "href": "posts/01_info_theory.html",
    "title": "Information Theory in Language Models (Part 1): Entropy, Cross-Entropy, and KL Divergence",
    "section": "",
    "text": "Self-Information\nInformation theory (Shannon 1948) provides a rigorous mathematical framework for quantifying “information” and “uncertainty.”\nEvery event carries a certain amount of information. For a random event, its information content can be intuitively understood as the “degree of surprise”: the lower the prior probability of an event, the more information it conveys when it actually occurs. Take weather as an example: * “It rained yesterday” is an established fact with zero surprise, so it contains zero information. * However, “it will rain tomorrow” remains uncertain, so when it happens, it carries actual information.\nImagine you are hiking deep in the mountains with absolutely no cell phone signal. You know nothing about tomorrow’s cloud cover, humidity, atmospheric pressure, or any other physical conditions. In such a state of extreme cognitive deprivation, the most conservative inference you can make, is that that the probabilities of tomorrow being sunny, cloudy, or rainy are evenly split at 1/3 each, based on the principle of symmetry. This reflects a severe lack of knowledge regarding the weather system’s underlying laws—to you, the system’s uncertainty is immense.\nIn contrast, if you check an authoritative weather forecast today and learn that the probability of a sunny day tomorrow is 0.99. Where does such high certainty come from? In fact, a massive amount of information has already been injected into the forecasting system in advance. Meteorologists have digested the original uncertainty using satellite images, historical weather models, and supercomputer simulations. Achieving such high certainty means that, during this prediction process, physical determinism has overwhelmingly outweighed randomness. From this we can draw a rather philosophical conclusion:\nInformation is essentially the reduction in uncertainty from learning an outcome.\nTo quantify how much uncertainty has been reduced, we need a metrics to measure the uncertainty. This leads to the mathematical definition of information. For a random variable (X) defined on a discrete outcome space () with probability mass function (p(x)), the amount of information gained by observing the event (X=x), the self-information,is defined as:\n\\[\nI(X=x) = -\\log p(x)\n\\]\nThe base of the logarithm is usuall 2 (unit: bit) or base (e) (unit: nat). (I(x)) measures the “amount of information” obtained when we observe a specific outcome (x). Returning to the weather example:\n\nExample 1: If “it rained yesterday” is a certain fact, \\(p(x=\\text{rainy}) = 1\\), then \\(I(x) = -\\ln(1) = 0\\). A certain event contains no information.\nExample 2: Suppose a weather station predicts that the prior probability of rain tomorrow is \\(p(x=\\text{rainy}) = 0.1\\). Then the self-information of the event “it rains tomorrow” is \\(I(X=\\text{rainy}) = -\\ln(0.1) \\approx 2.3\\) nats. This means that if it actually rains tomorrow, we will receive about 2.3 nats of “information” or “surprise.”\n\n\n\nShannon Entropy\nUsing the weather example, if the probability of rain is \\(p(x)\\), the information content of the event “rain tomorrow” is \\(-\\log(p(x))\\). If we enumerate all possible outcomes (rainy, cloudy, sunny) and compute the expectation of their information contents. This expectation is what we call entropy.\nMathematically, suppose an event (X) (such as tomorrow’s weather) has \\(n\\) possible outcomes with respective probabilities \\(p(x_i)\\). The expected information, or entropy, is expressed as:\n\\[\nH(X) = \\mathbb{E}_{x \\sim p}[I(x)] = -\\sum_{i=1}^{n} p(x_i) \\log p(x_i) \\quad \\text{(Discrete case)}\n\\]\nThe higher the entropy, the greater the system’s uncertainty. For instance, if the probabilities of tomorrow’s weather are: \\[p(x_1=\\text{rainy}) = 0.1,\\; p(x_2=\\text{cloudy}) = 0.3,\\; p(x_3=\\text{sunny}) = 0.6\\.\\]\nThe entropy is: \\[\nH(X) = -0.1 \\ln(0.1) - 0.3 \\ln(0.3) - 0.6 \\ln(0.6) \\approx 0.898 \\text{ nats}\n\\]\nThis value (approximate 0.898 nats) represents our measure of the average uncertainty regarding tomorrow’s weather.\n\n\nCross Entropy\nIn reality, we rarely know the true distribution \\(p\\); instead, we have a predicted distribution \\(q\\). For instance, we might rely on a weather app to predict tomorrow’s weather, but that prediction isn’t necessarily perfect. We can use cross-entropy \\(H(p, q)\\) to define the measurement cost required when we use a suboptimal predicted distribution \\(q\\) to represent the average information from the true distribution \\(p\\): \\[\nH(p, q) = \\mathbb{E}_{x \\sim p(x)}[-\\log q(x)] = -\\sum_{x} p(x) \\log q(x)\n\\] Note that the expectation is taken with respect to the true distribution \\(p\\), but the log probabilities come from the predictive model distribution \\(q\\).\nLet’s go back to the weather forecast example we looked at earlier. Suppose the true probabilities of sunny, cloudy, and rainy are \\(p = \\{0.1, 0.3, 0.6\\}\\). First, we calculate the entropy of the true distribution itself, which is the minimum average information required under optimal encoding (using base \\(e\\), unit: nats):\n\\[\nH(p) = -\\sum_{x} p(x) \\log p(x) = -(0.1\\ln0.1 + 0.3\\ln0.3 + 0.6\\ln0.6) \\approx 0.898\n\\] Now, suppose two weather apps provide different predicted distributions for tomorrow:\n\nModel A (More accurate): \\(q_A = \\{0.15, 0.25, 0.60\\}\\)\nModel B (Less accurate): \\(q_B = \\{0.70, 0.20, 0.10\\}\\)\n\nWe can calculate the cross-entropy of these two predictions relative to the true distribution:\n\nCross-entropy of Model A: \\(H(p,q_A) = -(0.1\\ln0.15 + 0.3\\ln0.25 + 0.6\\ln0.60) \\approx 0.912\\)\nCross-entropy of Model B: \\(H(p,q_B) = -(0.1\\ln0.70 + 0.3\\ln0.20 + 0.6\\ln0.10) \\approx 1.901\\)\n\nAs we can see, Model A’s cross-entropy (0.912) is much closer to the true entropy (0.897). A lower cross-entropy indicates that the predicted distribution is closer to the true distribution.\nBy generalizing the weather classification example, we arrive at the cross-entropy loss function—the workhorse behind most machine learning classifiers. When we train deep neural networks for tasks like image classification or language modeling, the central goal is to minimize this cross-entropy loss via backpropagation. In doing so, we drive the model’s predicted distribution qq to match the true label distribution pp as closely as possible.\n\n\nKL Divergence\nIn the weather prediction example above, a good prediction model (Model A) is clearly superior to a poor one (Model B). But what if we want to further quantify exactly how much extra information cost (i.e., cognitive bias or prediction error) we paid because the predicted distribution \\(q\\) is inaccurate compared to the ideal state \\(p\\)? This is exactly what KL Divergence answers (Kullback and Leibler 1951).\nIn information theory, there is a classic information cost decomposition:\n\\[\n\\underbrace{H(p, q)}_{\\text{Total predictive cost}} = \\underbrace{H(p)}_{\\text{Inherent randomness (minimum possible cost)}} + \\underbrace{D_{\\mathrm{KL}}(p \\| q)}_{\\text{Extra cost due to model inaccuracy}}\n\\]\nIntuitively:\n\n\\(H(p)\\) (entropy): this is the intrinsic physical noise of the world. Even if your model were omniscient, this part of uncertainty could never be eliminated.\n\\(D_{\\mathrm{KL}}(p \\| q)\\) (KL divergence): this is the “cognitive gap” between you and the real world. It measures the “distance” (asymmetric) between two probability distributions. the information redundancy or extra waste caused by the model’s imperfection.\n\nWe can then derive the formula of KL divergence:\n\\[\nD_{\\mathrm{KL}}(p \\| q) = H(p, q) - H(p) = \\sum_{i=1}^{n} p(x_i) \\log\\left(\\frac{p(x_i)}{q(x_i)}\\right)\n\\]\nUsing our running example, the true distribution \\(p=\\{0.1, 0.3, 0.6\\}\\) has an entropy \\(H(p) \\approx 0.897\\) nats.\nModel A (More accurate):\n\nPrediction: \\(q_A = \\{0.15, 0.25, 0.60\\}\\)\nCross-Entropy: \\(H(p, q_A) \\approx 0.912\\) nats\nKL Divergence: \\(D_{KL}(p \\parallel q_A) = 0.912 - 0.897 = 0.015\\) nats\nAnalysis: Information redundancy is extremely small (near 0), indicating Model A’s predictive efficiency is very close to the theoretical optimum.\n\nModel B (Less accurate):\n\nPrediction: \\(q_B = \\{0.70, 0.20, 0.10\\}\\)\nCross-Entropy: \\(H(p, q_B) \\approx 1.901\\) nats\nKL Divergence: \\(D_{KL}(p \\parallel q_B) = 1.901 - 0.897 = 1.004\\) nats\nAnalysis: Huge information redundancy. This means due to Model B’s severe bias, every time it encodes weather information, it wastes over 1 nat of information just dealing with its own errors.\n\nKL Divergence has two classic properties:\n\n: \\(D_{KL}(p \\parallel q) \\neq D_{KL}(q \\parallel p)\\). KL divergence is not a true geometric “distance metric” (which requires symmetry). It measures the information lost when using \\(q\\) to approximate \\(p\\), taking \\(p\\) as the baseline.\nNon-negativity: \\(D_{KL}(p \\parallel q) \\ge 0\\). According to Jensen’s inequality, the divergence is 0 if and only if \\(p=q\\). This reflects a common-sense principle in information theory: no predictive encoding can be better than the true distribution itself. Any imperfect model will inevitably incur extra information costs.\n\n\n\nBeyond Weather Forecasting: Applications in Machine Learning\nThe principles of entropy, cross-entropy, and KL divergence form a powerful “information-cost” framework that naturally extends into the core of modern machine learning, deep learning, and reinforcement learning.\nSupervised Learning: In classic single-label classification, the ground-truth label is typically represented as a one-hot vector. For the correct class \\(y^*\\), the true probability is \\(P(y^*|x) = 1\\) and zero for all other classes, resulting in a true distribution entropy of \\(H(P) = 0\\). Consequently, the cross-entropy loss used to train classifiers reduces entirely to the negative log-likelihood:\n\\[\n\\mathcal{L}_{\\mathrm{CE}} = H(P, Q) = -\\sum_{y} P(y|x) \\log Q(y|x) = -\\log Q(y^*|x).\n\\]\nBecause the inherent entropy \\(H(P) = 0\\), minimizing the cross-entropy is mathematically equivalent to minimizing the KL divergence \\(D_{\\mathrm{KL}}(P \\parallel Q)\\)—essentially forcing the model’s predicted distribution \\(Q\\) to map perfectly to the true distribution \\(P\\).\nGenerative Models: In generative models like Variational Autoencoders (VAEs) (Kingma and Welling 2013), KL divergence acts directly as an optimization objective to regularize the latent variable distribution. VAEs are trained by maximizing the Evidence Lower Bound (ELBO), formulated as: \\[\n\\mathrm{ELBO} = \\mathbb{E}_{z \\sim Q_\\phi(z|x)}[\\log P_\\theta(x|z)] - \\beta \\cdot D_{\\mathrm{KL}}(Q_\\phi(z|x) \\| P(z)).\n\\] The corresponding minimization loss is (_{} = -).\nTo reframe this as a minimization problem, the loss function becomes \\(\\mathcal{L}_{\\mathrm{VAE}} = -\\mathrm{ELBO}\\), which balances two critical components:\n\nThe Reconstruction Term: \\(\\mathbb{E}[\\log P_\\theta(x|z)]\\) ensures the decoder can accurately reconstruct the real data \\(x\\) from the latent variable \\(z\\) (typically measured via mean squared error or cross-entropy).\nThe Regularization Term: \\(D_{\\mathrm{KL}}(Q_\\phi(z|x) \\parallel P(z))\\) forces the encoder’s output posterior distribution \\(Q_\\phi(z|x)\\) to approximate a defined prior \\(P(z)\\) (such as a standard normal distribution \\(\\mathcal{N}(0, I)\\)).\n\nKnowledge Distillation:  Knowledge distillation(Hinton et al. 2015) aims to transfer the rich representations embedded in a large “teacher model” (such as DeepSeek, GPT, or Gemini) into a lightweight “student model.” To ensure the student learns not only the hard labels (0/1) but also the nuanced inter-class similarities discovered by the teacher, we introduce KL divergence to align their soft labels:\n\\[\n\\mathcal{L}_{\\mathrm{KD}} = \\alpha \\cdot \\mathcal{L}_{\\mathrm{CE}}(y, q_s) + (1-\\alpha) \\cdot T^2 \\cdot D_{\\mathrm{KL}}(p_t \\| p_s),\n\\] where the teacher’s and student’s smoothed probabilities at temperature \\(T\\) are defined as: \\[\np_t = \\mathrm{softmax}\\!\\left(\\frac{z_t}{T}\\right), \\quad p_s = \\mathrm{softmax}\\!\\left(\\frac{z_s}{T}\\right).\n\\]\nA higher temperature \\(T\\) yields a smoother distribution that reveals hidden inter-class relationships, while multiplying the term by \\(T^2\\) preserves the original gradient scale of the soft-label loss. By minimizing \\(D_{\\mathrm{KL}}(p_t \\parallel p_s)\\), the student model is forced to replicate the teacher’s reasoning logic and predictive confidence. This yields a significantly stronger generalization capability than could be achieved by fitting hard labels alone.\nReinforcement Learning:  In reinforcement learning (RL), KL divergence and entropy play distinct but equally critical roles in stabilizing training and promoting exploration.\n\nKL divergence for constraining policy updates: In the clipped version of Proximal Policy Optimization (PPO) (Schulman et al. 2017), the objective function is: \\[\n  \\mathcal{L}^{\\mathrm{CLIP}}(\\theta) = \\hat{\\mathbb{E}}_t \\left[ \\min\\left( r_t(\\theta) \\hat{A}_t,\\; \\mathrm{clip}(r_t(\\theta), 1-\\epsilon, 1+\\epsilon) \\hat{A}_t \\right) \\right],\n  \\] where \\(r_t(\\theta) = \\frac{\\pi_\\theta(a_t|s_t)}{\\pi_{\\theta_{\\mathrm{old}}}(a_t|s_t)}\\). By restricting the probability ratio \\(r_t(\\theta)\\) to the interval \\([1-\\epsilon, 1+\\epsilon]\\), the clipping operation implicitly bounds the KL divergence \\(D_{\\mathrm{KL}}(\\pi_{\\theta_{\\mathrm{old}}} \\parallel \\pi_\\theta)\\). This prevents drastic, destructive policy updates and ensures monotonic training stability.\nEntropy for encouraging exploration: To prevent a policy from prematurely converging to a deterministic, suboptimal solution, many policy gradient methods incorporate an entropy regularization term into the loss:\n\\[\n  \\mathcal{L} = \\mathbb{E}_t\\left[ \\log \\pi_\\theta(a_t|s_t) \\hat{A}_t \\right] + \\beta \\cdot H(\\pi_\\theta(\\cdot|s_t)),\n\\]\n\nSoft Actor-Critic (SAC) (Haarnoja et al. 2018) advances this concept by establishing a formal “maximum entropy” RL framework. SAC maximizes a weighted sum of the expected return and the policy’s entropy by optimizing the following objective:\\[J(\\pi) = \\sum_t \\mathbb{E}_{(s_t, a_t)} \\left[ r(s_t, a_t) + \\alpha H(\\pi(\\cdot|s_t)) \\right].\\]This formulation structurally guarantees a superior balance between exploration and exploitation, leading to more robust policies with enhanced generalization across varied environments.\n\n\nThe Physical Roots: Thermodynamics and Statistical Mechanics\nBefore wrapping up, let us revisit the origins of entropy and KL divergence in their birthplace: thermodynamics and statistical physics.\nThe Physical picture: particles and energy levels:  Imagine randomly throwing \\(N\\) small stones (analogous to independent particles) into \\(M\\) buckets (analogous to energy levels). The bucket each stone lands in is random, ultimately yielding an observed distribution where \\(n_i\\) is the number of stones in bucket \\(i\\). This process naturally generates a probability distribution where the proportion of stones in each bucket is \\(p_i = n_i / N\\).\nSuppose we only care about the final macrostate—how many stones end up in each bucket, regardless of which specific stone went where. We describe this macrostate using a vector \\(\\mathbf{n} = \\{n_1, n_2, \\dots, n_M\\}\\) under the strict constraint that \\(\\sum n_i = N\\). The question then becomes: How many microscopic arrangements (microstates) lead to this exact macrostate?\nThe first stone has \\(N\\) possible placements, the second \\(N-1\\), and so on, giving \\(N!\\) total permutations. However, because we do not care about the ordering of stones within the same bucket, we must divide out the redundant permutations by taking the factorial of each bucket’s occupation number. This gives us the total number of microstates (the multinomial coefficient) behind the macrostate:\n\\[W = \\frac{N!}{\\prod_{i=1}^{M} n_i!}\\]\nIn physics, \\(W\\) is known as the multiplicity. Ludwig Boltzmann famously proposed that the physical entropy of a system is proportional to the logarithm of its multiplicity. Therefore, we can define the physical entropy per particle as:\nIf we further assume that each bucket has an equal prior probability (i.e., a uniform distribution with probability (1/M)), then the total probability of observing the macrostate () follows a standard multinomial distribution:\n\\[H = \\frac{1}{N} \\log W\\]\nIf we assume each bucket has an equal prior probability of catching a stone (a uniform distribution with probability \\(1/M\\)), the total probability of observing the macrostate \\(\\mathbf{n}\\) follows a standard multinomial distribution:\n\\[P(n_1, n_2, \\dots, n_M) = W \\cdot \\left(\\frac{1}{M}\\right)^N\\]\nTaking the logarithm of this probability and dividing by \\(N\\), we obtain:\n\\[\\frac{1}{N} \\log P = \\frac{1}{N} \\log W + \\log\\!\\left(\\frac{1}{M}\\right)\\]\nBecause the second term is a fixed constant, we uncover a profound physical truth: finding the macrostate with the largest multiplicity \\(W\\) is mathematically equivalent to finding the state with the highest probability of occurring.\nUniform distribution to Shannon entropy:  Now, let’s explore what happens in the macroscopic limit as \\(N \\to \\infty\\), while keeping the bucket ratios \\(p_i = n_i/N\\) fixed. Expanding our entropy equation \\(H\\):\n\\[H = \\frac{1}{N} \\log(N!) - \\frac{1}{N} \\sum_{i=1}^{M} \\log(n_i!)\\]\nBy applying Stirling’s approximation (\\(\\log(x!) \\approx x \\log x - x\\)) for large numbers, the equation transforms into:\n\\[\nH \\approx \\frac{1}{N} \\left( N \\log N - N - \\sum_{i=1}^{M} (n_i \\log n_i - n_i) \\right)\n\\]\nGiven our constraint that \\(\\sum n_i = N\\), the linear \\(N\\) terms cancel out, simplifying the expression to:\n\\[\nH \\approx \\log N - \\frac{1}{N} \\sum_{i=1}^{M} n_i \\log n_i\n\\]\nNext, we substitute \\(n_i = N p_i\\) back into the sum:\n\\[\\begin{aligned}\nH &\\approx \\log N - \\frac{1}{N} \\sum_{i=1}^{M} (N p_i \\log (N p_i)) \\\\\n&= \\log N - \\sum_{i=1}^{M} p_i (\\log N + \\log p_i) \\\\\n&= \\log N - (\\log N) \\sum_{i=1}^{M} p_i - \\sum_{i=1}^{M} p_i \\log p_i\n\\end{aligned}\\]\nBecause the probabilities must sum to one (\\(\\sum p_i = 1\\)), the \\(\\log N\\) terms beautifully cancel each other out. Out of a classical physics thought experiment, we arrive directly at the formula for Shannon Information Entropy:\n\\[H = -\\sum_{i=1}^{M} p_i \\log p_i\\]\nFrom Non-uniform distribution to KL divergence: Now consider a twist: what if the inherent probabilities of a stone falling into each bucket are not equal? What if they follow a non-uniform prior distribution \\(q = \\{q_1, q_2, \\dots, q_M\\}\\) dictated by natural laws or environmental biases? In this scenario, the total probability of observing the macro-event becomes:\n\\[\nP(n_1, n_2, \\dots, n_M) = W \\prod_{i=1}^{M} (q_i)^{n_i}\n\\]\nIf we take the logarithm of this new non-uniform probability, divide by \\(N\\), and apply the exact same Stirling approximation logic as before, the derivation yields:\n\\[\n\\frac{1}{N} \\log P(n_1, n_2, \\dots, n_M) \\approx -\\sum_{i=1}^{M} p_i \\log\\left(\\frac{p_i}{q_i}\\right)\n\\] The right-hand side is instantly recognizable—it is precisely the negative Kullback-Leibler (KL) divergence, \\(-D_{\\mathrm{KL}}(p \\parallel q)\\).\nWe recognize the right-hand side as exactly the negative of the KL divergence, (-D_{}(p | q)).\nThe Takeaway: In the macroscopic limit, the logarithm of the probability of observing a specific macrostate is exactly the negative KL divergence between the observed distribution \\(p\\) and the prior distribution \\(q\\).\nBringing It Back to Machine Learning: This brings our story full circle. When we train a machine learning model by minimizing \\(D_{\\mathrm{KL}}(p \\parallel q)\\), the underlying physical interpretation is identical. By optimizing the model parameters \\(q\\), we are actively transforming the probability of observing our training data \\(p\\) from an “extremely unlikely, rare outlier” into the “most natural, highest-probability typical event.”\nThis foundational logic governs variational inference, knowledge distillation, and policy constraints in reinforcement learning. It also explains why physicists without formal AI backgrounds often make rapid, groundbreaking contributions to machine learning—beneath the code and the compute, the two domains speak the exact same language at their core.\n\n\nConclusion\nSince C. E. Shannon proposed the mathematical formulation of information entropy in 1948, this theoretical framework for quantifying uncertainty has laid the bedrock for information theory and statistical learning, continuously shaping the landscape of modern AI and ML.\nFrom the basic cognitive bias in weather forecasting to the cross-entropy loss that guides parameter updates in deep learning; from probability approximations in VAEs and distribution matching in knowledge distillation, to KL divergence regularization used to constrain LLMs(Ouyang et al. 2022) and RL training—the “information cost” metric system built on entropy, cross-entropy, and KL divergence is more than just the underlying grammar of ML algorithms. It has become our core analytical tool for understanding and navigating a world of uncertainty.\nMastering this framework not only clarifies “what is being calculated” during model optimization, but more importantly, uncovers the fundamental logic of “why it is calculated this way.”\n\n\n\n\n\nReferences\n\nHaarnoja, Tuomas, Aurick Zhou, Pieter Abbeel, and Sergey Levine. 2018. “Soft Actor-Critic: Off-Policy Maximum Entropy Deep Reinforcement Learning with a Stochastic Actor.” International Conference on Machine Learning, 1861–70.\n\n\nHinton, Geoffrey, Oriol Vinyals, and Jeff Dean. 2015. “Distilling the Knowledge in a Neural Network.” arXiv Preprint arXiv:1503.02531.\n\n\nKingma, Diederik P, and Max Welling. 2013. “Auto-Encoding Variational Bayes.” arXiv Preprint arXiv:1312.6114.\n\n\nKullback, Solomon, and Richard A Leibler. 1951. “On Information and Sufficiency.” The Annals of Mathematical Statistics 22 (1): 79–86.\n\n\nOuyang, Long, Jeffrey Wu, Xu Jiang, et al. 2022. “Training Language Models to Follow Instructions with Human Feedback.” Advances in Neural Information Processing Systems 35: 27730–44.\n\n\nSchulman, John, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. 2017. “Proximal Policy Optimization Algorithms.” arXiv Preprint arXiv:1707.06347.\n\n\nShannon, Claude Elwood. 1948. “A Mathematical Theory of Communication.” The Bell System Technical Journal 27 (3): 379–423."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Welcome to TokenTide AI’s Tech Blog 👋",
    "section": "",
    "text": "Order By\n      Default\n      \n        Date - Oldest\n      \n      \n        Date - Newest\n      \n      \n        Title\n      \n      \n        Author\n      \n    \n  \n    \n      \n      \n    \n\n\n\n\n\n\nTitle\n\n\n\nAuthor\n\n\n\nDate\n\n\n\n\n\n\n\n\nHarness Engineering for Optimization Modeling: Strategy, Implementation, and Expansion\n\n\nYitian Chen, Yanguang Chen, Siyu Shao\n\n\nAug 16, 2026\n\n\n\n\n\n\nOPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling\n\n\nYitian Chen, Cheng Cheng, Yinan Sun, Zi Ling, Dongdong Ge\n\n\nJul 23, 2026\n\n\n\n\n\n\nInformation Theory in Language Models (Part 1): Entropy, Cross-Entropy, and KL Divergence\n\n\nYitian Chen\n\n\nJun 11, 2026\n\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "posts/02_optengine.html",
    "href": "posts/02_optengine.html",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "Testing the Boundaries of LLMs in Optimization Modeling: Insights from the OPT-Engine Benchmark. In complex domains like logistics scheduling and financial engineering, high-stakes decisions rely on optimization modeling. The hardest part of bringing operations research into production isn’t solving the math—it’s translating human natural language into standardized mathematical models. This deceptively simple step requires AI models to excel at three hard tasks at once: understanding natural language, structuring abstract math, and executing precise computations\nIn recent years, large language models (LLMs) have demonstrated the ability to write code, solve mathematical problems, and even approach human-expert levels in some highly difficult reasoning tasks. However, a counter-intuitive phenomenon exists: when faced with classic modeling tasks in operations research, their performance degrades rapidly as problem complexity increases. To determine whether this phenomenon stems from a lack of precise computational ability or a failure to truly understand complex constraints and modeling logic, a recent research work introduced an extensible benchmark framework called OPT-Engine. Based on OPT-Engine, the research team focused on three core questions:\n\nIs pure-text reasoning sufficient? The team explored whether the pure-text reasoning (PTR) paradigm, based on classic mathematical chain-of-thought logic, can effectively handle optimization tasks. Experiments show that as task complexity increases, PTR experiences a severe robustness gap. The “effectiveness” of many current approaches largely stems from the sheer simplicity of existing benchmark tasks.\nIs text chain-of-thought reasoning combined with computational tools enough? The researchers tested if introducing external calculation tools (like SciPy) could compensate for PTR’s arithmetic weaknesses. Results indicate that while tools assist with localized calculations, they still struggle to resolve global optimization constraints and overarching modeling logic.\nWhere is the bottleneck of current mainstream paradigms? The current state-of-the-art (SOTA) paradigm—Solver-Integrated Reasoning (SIR)—outperforms pure-text reasoning but remains unsatisfactory on relatively complex optimization problems. Through controlled experiments, the team pinpointed the core bottleneck: it lies in whether LLMs can accurately understand natural language constraints and automatically translate them into precise mathematical models.\n\nThis research systematically evaluates the true performance of mainstream LLM reasoning paradigms on optimization modeling tasks. It reveals the key shortcoming of existing SOTA models in complex decision-making scenarios: the real difficulty is not “calculating the answer,” but correctly understanding and modeling constraints. This points out a clear direction for the future development of AI in operations research modeling.\nThe related research has been accepted by ICML 2026. The code and data are open-sourced on GitHub at https://github.com/Cardinal-Operations/OPTEngine. The paper is available at https://arxiv.org/abs/2601.19924.\n\n\n\n\nBefore OPT-Engine, many papers claimed LLMs achieved SOTA results on optimization tasks, but an undeniable fact remained: existing test questions were often too simple. They primarily consisted of fixed-scale, small “textbook” problems, such as the classic Traveling Salesperson Problem (TSP) that only required planning routes for 5 nodes. These tasks verify if AI is familiar with standard problem types, but they fail to simulate complex commercial environments involving thousands of variables. Conversely, feeding real-world industrial problems directly to LLMs—which could span millions of tokens in natural language—is neither realistic nor economical.\nTo address this, the research team proposed a systematic compromise based on the complexity of existing benchmarks. Inspired by LeetCode’s scalable complexity concept, OPT-Engine allows problem difficulty to scale slowly and controllably. This design allows researchers to track the trajectory of model capabilities as complexity grows, rather than just yielding a static score at a fixed difficulty level. For example, the number of cities in a TSP can be gradually expanded from 5 to 100, striking an ideal balance between the practical feasibility of text descriptions and the need for rigorous model capability evaluation.\nThe framework covers 10 classic operations research problem types across two major categories:\n\nLinear Programming (LP): This includes inventory management and portfolio optimization problems.\nMixed Integer Programming (MIP): This includes the Traveling Salesperson Problem (TSP) and the knapsack problem.\n\n\n\n\n\nThere are currently two representative routes in research on LLMs solving optimization problems.\nThe first is Pure-Text Reasoning (PTR), which naturally extends the classic mathematical reasoning chain-of-thought paradigm to optimization tasks. It does not rely on external tools and prompts the LLM to reason step-by-step to generate final results. Its advantage is being concise, end-to-end, and tool-free, with a typical example being Google’s paper Large Language Models as Optimizers.\nThe second route is Solver-Integrated Reasoning (SIR), which is the more mainstream approach for solving optimization tasks today. Examples include agentic methods like OptiMUS or fine-tuning methods like ORLM, SIRL, and OptMATH. The core idea is that the LLM understands the problem and generates the mathematical model or solver code, while the heavy numerical calculations are offloaded to professional external optimization solvers like Gurobi or COPT.\nOn simple problems, such as mainstream benchmarks prior to 2025, PTR and SIR perform comparably. However, system evaluations by the research team using top-tier LLMs from early 2026 within the OPT-Engine framework revealed that PTR’s accuracy experiences a cliff-like drop once the problem scale increases. In contrast, SIR demonstrates stronger stability at higher complexities by offloading exact calculations to external solvers.\nThis comparison yields two key conclusions:\n\nThe Performance Ceiling of PTR: Without external calculation tools, PTR quickly hits its ceiling as complexity rises. This degradation highlights a fundamental mismatch between the text reasoning capabilities LLMs excel at and the precise algorithmic calculations required by optimization problems.\nThe Paradigm Advantage of SIR: By generating executable solver code and calling tool interfaces, LLMs consistently outperform tool-free pure-text solutions. This “task offloading” strategy shifts the precise computational burden to deterministic solvers, allowing the model to maintain strong robustness in complexity ranges where PTR has already failed. This proves that in complex optimization tasks, LLMs are better suited to play the role of “modeling assistants” or “tool coordinators” rather than completely replacing the optimization algorithms themselves.\n\nThe study explicitly concludes that SIR is the necessary framework for tackling high-complexity, industrial-grade optimization challenges moving forward.\n\n\n\n\nSince the performance of chain-of-thought-based PTR degrades significantly as problem scale increases due to precision demands, it is natural to wonder if providing external calculation tools would solve the issue. To investigate whether a “calculator” fixes PTR’s flaws, the research team designed a rigorous three-stage system analysis and experiment:\n\nTrajectory and Pattern Analysis: Using TSP as a case study, the team analyzed token-level response patterns of LLM reply trajectories under the PTR mode, combining this with full reasoning trajectories for case studies.\nError Decomposition Experiment: Errors in optimization tasks were explicitly divided into constraint infeasibility errors and other errors (like imprecise calculations). By defining “infeasibility” as violating the feasible region, they decoupled structural modeling failures from arithmetic noise, enabling strict categorization of PTR errors.\nCompPTR Paradigm Design: CompPTR acts as an enhanced version of PTR where the model reasons primarily via natural language but is permitted to use Python tools for auxiliary calculations. It provides the model a “calculator” without allowing it to hand the entire problem over to a professional solver. This helps answer whether external tools improve global feasibility or only patch local arithmetic weaknesses while underlying global logical flaws remain unresolved.\n\n\n\nBased on a fine-grained analysis of DeepSeek-V3.2’s output behavior on the TSP problem, the research team mapped the relationship between average output length (tokens) and accuracy:\n\nSmall-Scale Phase: The model proportionally increases its “reasoning effort” as problem complexity rises, with output tokens growing roughly linearly. Because the search space (a scale of (n-1)!) remains manageable, the model relies on explicitly enumerating all Hamiltonian cycles for precise solving, resulting in high accuracy.\nPost-Critical Threshold Phase: When the number of cities nears a critical point right before a cliff-like drop in accuracy, the model exhibits counter-intuitive behavior: token budgets actually decrease despite the increased task difficulty.\n\nA distinct strategy shift is visible: moving from precise solving to heuristic estimation.\n\nAfter crossing the threshold, the model actively abandons exact enumeration and switches to lightweight heuristic methods like Nearest Neighbor and Cheapest Insertion to obtain approximate solutions. Token consumption levels off, and solution quality subsequently drops.\nThis adaptive behavior somewhat mimics human expert practices: when precise solving becomes computationally unfeasible, the model swiftly switches to rapid heuristic algorithms.\n\nThis dynamic mechanism explains the reported effectiveness of previous PTR-based end-to-end optimizers, providing a mechanistic explanation for their success. Their success stems not from truly “precisely solving” complex problems, but because the model internally learned to dynamically switch to heuristic methods, trading optimal solutions for sub-optimal but practical approximations with acceptable evaluation metrics. Essentially, they score well because the evaluation standard implicitly shifts to an “open-book, merely passing” criteria.\n\n\n\nTo strictly verify the observation that PTR behaves more like a stochastic approximation, the team categorized errors into two distinct types:\n\nInfeasibility Errors: Violations of constraint conditions, indicating a structural modeling failure.\nOther Errors: Errors primarily stemming from calculation inaccuracies, such as arithmetic mistakes.\n\nThe comparison revealed completely different failure modes:\n\nFor the SIR Paradigm: Accuracy is strictly governed by structural feasibility. Because numerical calculations are fully offloaded to external solvers, SIR’s bottleneck is purely structural. As long as the model provides the correct logic mapping (constraints and objective functions are error-free), the solver guarantees optimality. If the structure is right, the result is right.\nFor the PTR Paradigm: PTR exhibits a dual-failure trajectory. As complexity increases, PTR fails in both feasibility and computational precision. First, it suffers a “reasoning collapse” where it cannot satisfy global logic constraints, causing feasibility rates to plummet. Second, it suffers from “insufficient precision,” meaning even if the model accurately understands constraints, it lacks the deterministic numerical precision required for an exact optimal solution.\n\nIn short, SIR’s failure is “one-dimensional” (wrong only if the structure is wrong), while PTR’s failure is “two-dimensional” (the structure can be wrong, and the numbers are hard to get precise).\n\n\n\nThe CompPTR framework permits the model to use Python-based computing primitives (like scipy or itertools) for auxiliary numerical calculations, but it explicitly forbids the model from utilizing external optimization solvers (like Gurobi or COPT) for global optimization. Experiments show that CompPTR’s performance sits in the middle—better than PTR, but failing to reach the heights of SIR.\nObservations of CompPTR highlight the following:\n\nSignificant Improvements in Feasibility and Precision: External computational support not only mitigates PTR’s inherent arithmetic errors but also reduces infeasibility errors previously caused by reasoning failures.\nInability to Match SIR: CompPTR still encounters reasoning collapse as complexity scales. While it successfully uses tools for discrete subtasks, it fails to internalize the global optimality requirements of optimization problems. It struggles to maintain a coherent global optimization strategy, leading to continued performance degradation as problem scales grow.\n\nUltimately, computational tools can fix localized issues, but they cannot cure global structural deficiencies. Local arithmetic precision is useless without a global modeling perspective in complex optimization tasks.\n\n\n\n\n\nIf SIR is the reliable framework for large-scale industrial problems, why do LLMs—which perform almost perfectly on human intellectual math tasks like the Math Olympiad—still underperform on textbook-level operations research problems? To locate the true bottleneck, the research team designed three controlled experiments:\n\n\n\n\n\n\n\n\nExperiment Type\nMethodology\nConclusion\n\n\n\n\nLinguistic Complexity Control\nBuilt derivative versions of descriptive templates with increasing syntactic and vocabulary complexity using Perplexity (PPL) as a metric, while keeping the underlying numerical instances and constraint sets identical.\n\n\n\n\n| Model accuracy does not significantly drop as language complexity (PPL) increases. The model’s failure is not due to an “inability to read complex expressions.”\n| | Objective Function Perturbation | Applied linear perturbations (like adding a constant term) to the original objective function to alter the optimization criteria.\n| Model performance is almost entirely unaffected. Objective function variations are not the primary bottleneck.\n| | Constraint Enhancement | Added mathematically simple but semantically demanding constraints, such as excluding a specific edge in TSP, adding mutual exclusion in the knapsack problem, or setting order caps in inventory management. These are non-binding or trivial constraints that do not change asymptotic complexity, only the semantic modeling burden.\n| Accuracy drops significantly for both the SIR and PTR paradigms.\n|\nCore Interpretation: This counter-intuitive pattern indicates that current LLMs may not have truly internalized or robustly reasoned through problem constraints. Instead, they heavily rely on standard textbook templates (like classic TSP and knapsack problems) that are abundant in their training data. Standard versions closely match their training data, but constraint-enhanced versions introduce condition combinations the models are unlikely to have seen.\nReal-world optimization tasks rarely appear in “clean” standard forms and are almost always accompanied by customized operational or regulatory constraints. This reveals an awkward truth: currently, LLMs are largely “memorizing” problems. They excel at familiar standard types but fail to handle slightly modified, customized constraints. The automated modeling of constraints is currently the biggest bottleneck in AI-driven operations research.\n\n\n\n\nOPT-Engine is a highly inspiring piece of work in the field of LLMs solving optimization problems. It builds a quantifiable and scalable bridge between textbook “toy examples” and complex real-world industrial problems. By leveraging classic LP and MIP problems and adapting LeetCode’s complexity scaling, it allows optimization difficulty to rise slowly and controllably to scientifically evaluate the true performance of different reasoning paradigms.\nThe core conclusions of the study are summarized in three main points:\n\nThe Golden Paradigm for Industrial Application: At the current stage, “LLM Brain + Professional Solver Execution” (SIR) is the most capable paradigm capable of gaining a foothold in real industrial scenarios.\nThe Fatal Bottleneck of SIR: Even SOTA SIR systems have inherent flaws in understanding natural language descriptions of complex problems and logically translating complex, non-standard constraints.\nViable Future Directions: A promising improvement for solving complex problems is the Meta-Solver. This approach aims to make LLMs act like human experts, automatically selecting the most appropriate solving strategy based on prior experience for different problems (e.g., automatically switching to heuristic algorithms for multi-node TSPs).\n\nThe future direction worth exploring is not forcing LLMs to directly “replace solvers,” but rather enabling them to become more reliable optimization modeling and tool-coordination systems. They need to understand problem structures, identify constraint types, select suitable algorithms, and call upon professional solvers or heuristics when necessary. In this sense, OPT-Engine not only diagnoses the current boundaries of LLMs in optimization modeling, but it also points the way forward for the next generation of AI optimization systems: moving from “solving standard problems” to “understanding real constraints,” and transitioning from “directly giving answers” to “intelligent modeling and tool synergy”.\n\n\nInformation theory (Shannon 1948) provides a rigorous mathematical framework for quantifying “information” and “uncertainty.”\nEvery event carries a certain amount of information. For a random event, its information content can be intuitively understood as the “degree of surprise”: the lower the prior probability of an event, the more information it conveys when it actually occurs. Take weather as an example: * “It rained yesterday” is an established fact with zero surprise, so it contains zero information. * However, “it will rain tomorrow” remains uncertain, so when it happens, it carries actual information.\nImagine you are hiking deep in the mountains with absolutely no cell phone signal. You know nothing about tomorrow’s cloud cover, humidity, atmospheric pressure, or any other physical conditions. In such a state of extreme cognitive deprivation, the most conservative inference you can make, is that that the probabilities of tomorrow being sunny, cloudy, or rainy are evenly split at 1/3 each, based on the principle of symmetry. This reflects a severe lack of knowledge regarding the weather system’s underlying laws—to you, the system’s uncertainty is immense.\nIn contrast, if you check an authoritative weather forecast today and learn that the probability of a sunny day tomorrow is 0.99. Where does such high certainty come from? In fact, a massive amount of information has already been injected into the forecasting system in advance. Meteorologists have digested the original uncertainty using satellite images, historical weather models, and supercomputer simulations. Achieving such high certainty means that, during this prediction process, physical determinism has overwhelmingly outweighed randomness. From this we can draw a rather philosophical conclusion:\nInformation is essentially the reduction in uncertainty from learning an outcome.\nTo quantify how much uncertainty has been reduced, we need a metrics to measure the uncertainty. This leads to the mathematical definition of information. For a random variable (X) defined on a discrete outcome space () with probability mass function (p(x)), the amount of information gained by observing the event (X=x), the self-information,is defined as:\n\\[\nI(X=x) = -\\log p(x)\n\\]\nThe base of the logarithm is usuall 2 (unit: bit) or base (e) (unit: nat). (I(x)) measures the “amount of information” obtained when we observe a specific outcome (x). Returning to the weather example:\n\nExample 1: If “it rained yesterday” is a certain fact, \\(p(x=\\text{rainy}) = 1\\), then \\(I(x) = -\\ln(1) = 0\\). A certain event contains no information.\nExample 2: Suppose a weather station predicts that the prior probability of rain tomorrow is \\(p(x=\\text{rainy}) = 0.1\\). Then the self-information of the event “it rains tomorrow” is \\(I(X=\\text{rainy}) = -\\ln(0.1) \\approx 2.3\\) nats. This means that if it actually rains tomorrow, we will receive about 2.3 nats of “information” or “surprise.”\n\n\n\n\nUsing the weather example, if the probability of rain is \\(p(x)\\), the information content of the event “rain tomorrow” is \\(-\\log(p(x))\\). If we enumerate all possible outcomes (rainy, cloudy, sunny) and compute the expectation of their information contents. This expectation is what we call entropy.\nMathematically, suppose an event (X) (such as tomorrow’s weather) has \\(n\\) possible outcomes with respective probabilities \\(p(x_i)\\). The expected information, or entropy, is expressed as:\n\\[\nH(X) = \\mathbb{E}_{x \\sim p}[I(x)] = -\\sum_{i=1}^{n} p(x_i) \\log p(x_i) \\quad \\text{(Discrete case)}\n\\]\nThe higher the entropy, the greater the system’s uncertainty. For instance, if the probabilities of tomorrow’s weather are: \\[p(x_1=\\text{rainy}) = 0.1,\\; p(x_2=\\text{cloudy}) = 0.3,\\; p(x_3=\\text{sunny}) = 0.6\\.\\]\nThe entropy is: \\[\nH(X) = -0.1 \\ln(0.1) - 0.3 \\ln(0.3) - 0.6 \\ln(0.6) \\approx 0.898 \\text{ nats}\n\\]\nThis value (approximate 0.898 nats) represents our measure of the average uncertainty regarding tomorrow’s weather.\n\n\n\nIn reality, we rarely know the true distribution \\(p\\); instead, we have a predicted distribution \\(q\\). For instance, we might rely on a weather app to predict tomorrow’s weather, but that prediction isn’t necessarily perfect. We can use cross-entropy \\(H(p, q)\\) to define the measurement cost required when we use a suboptimal predicted distribution \\(q\\) to represent the average information from the true distribution \\(p\\): \\[\nH(p, q) = \\mathbb{E}_{x \\sim p(x)}[-\\log q(x)] = -\\sum_{x} p(x) \\log q(x)\n\\] Note that the expectation is taken with respect to the true distribution \\(p\\), but the log probabilities come from the predictive model distribution \\(q\\).\nLet’s go back to the weather forecast example we looked at earlier. Suppose the true probabilities of sunny, cloudy, and rainy are \\(p = \\{0.1, 0.3, 0.6\\}\\). First, we calculate the entropy of the true distribution itself, which is the minimum average information required under optimal encoding (using base \\(e\\), unit: nats):\n\\[\nH(p) = -\\sum_{x} p(x) \\log p(x) = -(0.1\\ln0.1 + 0.3\\ln0.3 + 0.6\\ln0.6) \\approx 0.898\n\\] Now, suppose two weather apps provide different predicted distributions for tomorrow:\n\nModel A (More accurate): \\(q_A = \\{0.15, 0.25, 0.60\\}\\)\nModel B (Less accurate): \\(q_B = \\{0.70, 0.20, 0.10\\}\\)\n\nWe can calculate the cross-entropy of these two predictions relative to the true distribution:\n\nCross-entropy of Model A: \\(H(p,q_A) = -(0.1\\ln0.15 + 0.3\\ln0.25 + 0.6\\ln0.60) \\approx 0.912\\)\nCross-entropy of Model B: \\(H(p,q_B) = -(0.1\\ln0.70 + 0.3\\ln0.20 + 0.6\\ln0.10) \\approx 1.901\\)\n\nAs we can see, Model A’s cross-entropy (0.912) is much closer to the true entropy (0.897). A lower cross-entropy indicates that the predicted distribution is closer to the true distribution.\nBy generalizing the weather classification example, we arrive at the cross-entropy loss function—the workhorse behind most machine learning classifiers. When we train deep neural networks for tasks like image classification or language modeling, the central goal is to minimize this cross-entropy loss via backpropagation. In doing so, we drive the model’s predicted distribution qq to match the true label distribution pp as closely as possible.\n\n\n\nIn the weather prediction example above, a good prediction model (Model A) is clearly superior to a poor one (Model B). But what if we want to further quantify exactly how much extra information cost (i.e., cognitive bias or prediction error) we paid because the predicted distribution \\(q\\) is inaccurate compared to the ideal state \\(p\\)? This is exactly what KL Divergence answers (Kullback and Leibler 1951).\nIn information theory, there is a classic information cost decomposition:\n\\[\n\\underbrace{H(p, q)}_{\\text{Total predictive cost}} = \\underbrace{H(p)}_{\\text{Inherent randomness (minimum possible cost)}} + \\underbrace{D_{\\mathrm{KL}}(p \\| q)}_{\\text{Extra cost due to model inaccuracy}}\n\\]\nIntuitively:\n\n\\(H(p)\\) (entropy): this is the intrinsic physical noise of the world. Even if your model were omniscient, this part of uncertainty could never be eliminated.\n\\(D_{\\mathrm{KL}}(p \\| q)\\) (KL divergence): this is the “cognitive gap” between you and the real world. It measures the “distance” (asymmetric) between two probability distributions. the information redundancy or extra waste caused by the model’s imperfection.\n\nWe can then derive the formula of KL divergence:\n\\[\nD_{\\mathrm{KL}}(p \\| q) = H(p, q) - H(p) = \\sum_{i=1}^{n} p(x_i) \\log\\left(\\frac{p(x_i)}{q(x_i)}\\right)\n\\]\nUsing our running example, the true distribution \\(p=\\{0.1, 0.3, 0.6\\}\\) has an entropy \\(H(p) \\approx 0.897\\) nats.\nModel A (More accurate):\n\nPrediction: \\(q_A = \\{0.15, 0.25, 0.60\\}\\)\nCross-Entropy: \\(H(p, q_A) \\approx 0.912\\) nats\nKL Divergence: \\(D_{KL}(p \\parallel q_A) = 0.912 - 0.897 = 0.015\\) nats\nAnalysis: Information redundancy is extremely small (near 0), indicating Model A’s predictive efficiency is very close to the theoretical optimum.\n\nModel B (Less accurate):\n\nPrediction: \\(q_B = \\{0.70, 0.20, 0.10\\}\\)\nCross-Entropy: \\(H(p, q_B) \\approx 1.901\\) nats\nKL Divergence: \\(D_{KL}(p \\parallel q_B) = 1.901 - 0.897 = 1.004\\) nats\nAnalysis: Huge information redundancy. This means due to Model B’s severe bias, every time it encodes weather information, it wastes over 1 nat of information just dealing with its own errors.\n\nKL Divergence has two classic properties:\n\n: \\(D_{KL}(p \\parallel q) \\neq D_{KL}(q \\parallel p)\\). KL divergence is not a true geometric “distance metric” (which requires symmetry). It measures the information lost when using \\(q\\) to approximate \\(p\\), taking \\(p\\) as the baseline.\nNon-negativity: \\(D_{KL}(p \\parallel q) \\ge 0\\). According to Jensen’s inequality, the divergence is 0 if and only if \\(p=q\\). This reflects a common-sense principle in information theory: no predictive encoding can be better than the true distribution itself. Any imperfect model will inevitably incur extra information costs.\n\n\n\n\nThe principles of entropy, cross-entropy, and KL divergence form a powerful “information-cost” framework that naturally extends into the core of modern machine learning, deep learning, and reinforcement learning.\nSupervised Learning: In classic single-label classification, the ground-truth label is typically represented as a one-hot vector. For the correct class \\(y^*\\), the true probability is \\(P(y^*|x) = 1\\) and zero for all other classes, resulting in a true distribution entropy of \\(H(P) = 0\\). Consequently, the cross-entropy loss used to train classifiers reduces entirely to the negative log-likelihood:\n\\[\n\\mathcal{L}_{\\mathrm{CE}} = H(P, Q) = -\\sum_{y} P(y|x) \\log Q(y|x) = -\\log Q(y^*|x).\n\\]\nBecause the inherent entropy \\(H(P) = 0\\), minimizing the cross-entropy is mathematically equivalent to minimizing the KL divergence \\(D_{\\mathrm{KL}}(P \\parallel Q)\\)—essentially forcing the model’s predicted distribution \\(Q\\) to map perfectly to the true distribution \\(P\\).\nGenerative Models: In generative models like Variational Autoencoders (VAEs) (Kingma and Welling 2013), KL divergence acts directly as an optimization objective to regularize the latent variable distribution. VAEs are trained by maximizing the Evidence Lower Bound (ELBO), formulated as: \\[\n\\mathrm{ELBO} = \\mathbb{E}_{z \\sim Q_\\phi(z|x)}[\\log P_\\theta(x|z)] - \\beta \\cdot D_{\\mathrm{KL}}(Q_\\phi(z|x) \\| P(z)).\n\\] The corresponding minimization loss is (_{} = -).\nTo reframe this as a minimization problem, the loss function becomes \\(\\mathcal{L}_{\\mathrm{VAE}} = -\\mathrm{ELBO}\\), which balances two critical components:\n\nThe Reconstruction Term: \\(\\mathbb{E}[\\log P_\\theta(x|z)]\\) ensures the decoder can accurately reconstruct the real data \\(x\\) from the latent variable \\(z\\) (typically measured via mean squared error or cross-entropy).\nThe Regularization Term: \\(D_{\\mathrm{KL}}(Q_\\phi(z|x) \\parallel P(z))\\) forces the encoder’s output posterior distribution \\(Q_\\phi(z|x)\\) to approximate a defined prior \\(P(z)\\) (such as a standard normal distribution \\(\\mathcal{N}(0, I)\\)).\n\nKnowledge Distillation:  Knowledge distillation(Hinton et al. 2015) aims to transfer the rich representations embedded in a large “teacher model” (such as DeepSeek, GPT, or Gemini) into a lightweight “student model.” To ensure the student learns not only the hard labels (0/1) but also the nuanced inter-class similarities discovered by the teacher, we introduce KL divergence to align their soft labels:\n\\[\n\\mathcal{L}_{\\mathrm{KD}} = \\alpha \\cdot \\mathcal{L}_{\\mathrm{CE}}(y, q_s) + (1-\\alpha) \\cdot T^2 \\cdot D_{\\mathrm{KL}}(p_t \\| p_s),\n\\] where the teacher’s and student’s smoothed probabilities at temperature \\(T\\) are defined as: \\[\np_t = \\mathrm{softmax}\\!\\left(\\frac{z_t}{T}\\right), \\quad p_s = \\mathrm{softmax}\\!\\left(\\frac{z_s}{T}\\right).\n\\]\nA higher temperature \\(T\\) yields a smoother distribution that reveals hidden inter-class relationships, while multiplying the term by \\(T^2\\) preserves the original gradient scale of the soft-label loss. By minimizing \\(D_{\\mathrm{KL}}(p_t \\parallel p_s)\\), the student model is forced to replicate the teacher’s reasoning logic and predictive confidence. This yields a significantly stronger generalization capability than could be achieved by fitting hard labels alone.\nReinforcement Learning:  In reinforcement learning (RL), KL divergence and entropy play distinct but equally critical roles in stabilizing training and promoting exploration.\n\nKL divergence for constraining policy updates: In the clipped version of Proximal Policy Optimization (PPO) (Schulman et al. 2017), the objective function is: \\[\n  \\mathcal{L}^{\\mathrm{CLIP}}(\\theta) = \\hat{\\mathbb{E}}_t \\left[ \\min\\left( r_t(\\theta) \\hat{A}_t,\\; \\mathrm{clip}(r_t(\\theta), 1-\\epsilon, 1+\\epsilon) \\hat{A}_t \\right) \\right],\n  \\] where \\(r_t(\\theta) = \\frac{\\pi_\\theta(a_t|s_t)}{\\pi_{\\theta_{\\mathrm{old}}}(a_t|s_t)}\\). By restricting the probability ratio \\(r_t(\\theta)\\) to the interval \\([1-\\epsilon, 1+\\epsilon]\\), the clipping operation implicitly bounds the KL divergence \\(D_{\\mathrm{KL}}(\\pi_{\\theta_{\\mathrm{old}}} \\parallel \\pi_\\theta)\\). This prevents drastic, destructive policy updates and ensures monotonic training stability.\nEntropy for encouraging exploration: To prevent a policy from prematurely converging to a deterministic, suboptimal solution, many policy gradient methods incorporate an entropy regularization term into the loss:\n\\[\n  \\mathcal{L} = \\mathbb{E}_t\\left[ \\log \\pi_\\theta(a_t|s_t) \\hat{A}_t \\right] + \\beta \\cdot H(\\pi_\\theta(\\cdot|s_t)),\n\\]\n\nSoft Actor-Critic (SAC) (Haarnoja et al. 2018) advances this concept by establishing a formal “maximum entropy” RL framework. SAC maximizes a weighted sum of the expected return and the policy’s entropy by optimizing the following objective:\\[J(\\pi) = \\sum_t \\mathbb{E}_{(s_t, a_t)} \\left[ r(s_t, a_t) + \\alpha H(\\pi(\\cdot|s_t)) \\right].\\]This formulation structurally guarantees a superior balance between exploration and exploitation, leading to more robust policies with enhanced generalization across varied environments.\n\n\n\nBefore wrapping up, let us revisit the origins of entropy and KL divergence in their birthplace: thermodynamics and statistical physics.\nThe Physical picture: particles and energy levels:  Imagine randomly throwing \\(N\\) small stones (analogous to independent particles) into \\(M\\) buckets (analogous to energy levels). The bucket each stone lands in is random, ultimately yielding an observed distribution where \\(n_i\\) is the number of stones in bucket \\(i\\). This process naturally generates a probability distribution where the proportion of stones in each bucket is \\(p_i = n_i / N\\).\nSuppose we only care about the final macrostate—how many stones end up in each bucket, regardless of which specific stone went where. We describe this macrostate using a vector \\(\\mathbf{n} = \\{n_1, n_2, \\dots, n_M\\}\\) under the strict constraint that \\(\\sum n_i = N\\). The question then becomes: How many microscopic arrangements (microstates) lead to this exact macrostate?\nThe first stone has \\(N\\) possible placements, the second \\(N-1\\), and so on, giving \\(N!\\) total permutations. However, because we do not care about the ordering of stones within the same bucket, we must divide out the redundant permutations by taking the factorial of each bucket’s occupation number. This gives us the total number of microstates (the multinomial coefficient) behind the macrostate:\n\\[W = \\frac{N!}{\\prod_{i=1}^{M} n_i!}\\]\nIn physics, \\(W\\) is known as the multiplicity. Ludwig Boltzmann famously proposed that the physical entropy of a system is proportional to the logarithm of its multiplicity. Therefore, we can define the physical entropy per particle as:\nIf we further assume that each bucket has an equal prior probability (i.e., a uniform distribution with probability (1/M)), then the total probability of observing the macrostate () follows a standard multinomial distribution:\n\\[H = \\frac{1}{N} \\log W\\]\nIf we assume each bucket has an equal prior probability of catching a stone (a uniform distribution with probability \\(1/M\\)), the total probability of observing the macrostate \\(\\mathbf{n}\\) follows a standard multinomial distribution:\n\\[P(n_1, n_2, \\dots, n_M) = W \\cdot \\left(\\frac{1}{M}\\right)^N\\]\nTaking the logarithm of this probability and dividing by \\(N\\), we obtain:\n\\[\\frac{1}{N} \\log P = \\frac{1}{N} \\log W + \\log\\!\\left(\\frac{1}{M}\\right)\\]\nBecause the second term is a fixed constant, we uncover a profound physical truth: finding the macrostate with the largest multiplicity \\(W\\) is mathematically equivalent to finding the state with the highest probability of occurring.\nUniform distribution to Shannon entropy:  Now, let’s explore what happens in the macroscopic limit as \\(N \\to \\infty\\), while keeping the bucket ratios \\(p_i = n_i/N\\) fixed. Expanding our entropy equation \\(H\\):\n\\[H = \\frac{1}{N} \\log(N!) - \\frac{1}{N} \\sum_{i=1}^{M} \\log(n_i!)\\]\nBy applying Stirling’s approximation (\\(\\log(x!) \\approx x \\log x - x\\)) for large numbers, the equation transforms into:\n\\[\nH \\approx \\frac{1}{N} \\left( N \\log N - N - \\sum_{i=1}^{M} (n_i \\log n_i - n_i) \\right)\n\\]\nGiven our constraint that \\(\\sum n_i = N\\), the linear \\(N\\) terms cancel out, simplifying the expression to:\n\\[\nH \\approx \\log N - \\frac{1}{N} \\sum_{i=1}^{M} n_i \\log n_i\n\\]\nNext, we substitute \\(n_i = N p_i\\) back into the sum:\n\\[\\begin{aligned}\nH &\\approx \\log N - \\frac{1}{N} \\sum_{i=1}^{M} (N p_i \\log (N p_i)) \\\\\n&= \\log N - \\sum_{i=1}^{M} p_i (\\log N + \\log p_i) \\\\\n&= \\log N - (\\log N) \\sum_{i=1}^{M} p_i - \\sum_{i=1}^{M} p_i \\log p_i\n\\end{aligned}\\]\nBecause the probabilities must sum to one (\\(\\sum p_i = 1\\)), the \\(\\log N\\) terms beautifully cancel each other out. Out of a classical physics thought experiment, we arrive directly at the formula for Shannon Information Entropy:\n\\[H = -\\sum_{i=1}^{M} p_i \\log p_i\\]\nFrom Non-uniform distribution to KL divergence: Now consider a twist: what if the inherent probabilities of a stone falling into each bucket are not equal? What if they follow a non-uniform prior distribution \\(q = \\{q_1, q_2, \\dots, q_M\\}\\) dictated by natural laws or environmental biases? In this scenario, the total probability of observing the macro-event becomes:\n\\[\nP(n_1, n_2, \\dots, n_M) = W \\prod_{i=1}^{M} (q_i)^{n_i}\n\\]\nIf we take the logarithm of this new non-uniform probability, divide by \\(N\\), and apply the exact same Stirling approximation logic as before, the derivation yields:\n\\[\n\\frac{1}{N} \\log P(n_1, n_2, \\dots, n_M) \\approx -\\sum_{i=1}^{M} p_i \\log\\left(\\frac{p_i}{q_i}\\right)\n\\] The right-hand side is instantly recognizable—it is precisely the negative Kullback-Leibler (KL) divergence, \\(-D_{\\mathrm{KL}}(p \\parallel q)\\).\nWe recognize the right-hand side as exactly the negative of the KL divergence, (-D_{}(p | q)).\nThe Takeaway: In the macroscopic limit, the logarithm of the probability of observing a specific macrostate is exactly the negative KL divergence between the observed distribution \\(p\\) and the prior distribution \\(q\\).\nBringing It Back to Machine Learning: This brings our story full circle. When we train a machine learning model by minimizing \\(D_{\\mathrm{KL}}(p \\parallel q)\\), the underlying physical interpretation is identical. By optimizing the model parameters \\(q\\), we are actively transforming the probability of observing our training data \\(p\\) from an “extremely unlikely, rare outlier” into the “most natural, highest-probability typical event.”\nThis foundational logic governs variational inference, knowledge distillation, and policy constraints in reinforcement learning. It also explains why physicists without formal AI backgrounds often make rapid, groundbreaking contributions to machine learning—beneath the code and the compute, the two domains speak the exact same language at their core.\n\n\n\nSince C. E. Shannon proposed the mathematical formulation of information entropy in 1948, this theoretical framework for quantifying uncertainty has laid the bedrock for information theory and statistical learning, continuously shaping the landscape of modern AI and ML.\nFrom the basic cognitive bias in weather forecasting to the cross-entropy loss that guides parameter updates in deep learning; from probability approximations in VAEs and distribution matching in knowledge distillation, to KL divergence regularization used to constrain LLMs(Ouyang et al. 2022) and RL training—the “information cost” metric system built on entropy, cross-entropy, and KL divergence is more than just the underlying grammar of ML algorithms. It has become our core analytical tool for understanding and navigating a world of uncertainty.\nMastering this framework not only clarifies “what is being calculated” during model optimization, but more importantly, uncovers the fundamental logic of “why it is calculated this way.”"
  },
  {
    "objectID": "posts/02_optengine.html#introduction",
    "href": "posts/02_optengine.html#introduction",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "Testing the Boundaries of LLMs in Optimization Modeling: Insights from the OPT-Engine Benchmark. In complex domains like logistics scheduling and financial engineering, high-stakes decisions rely on optimization modeling. The hardest part of bringing operations research into production isn’t solving the math—it’s translating human natural language into standardized mathematical models. This deceptively simple step requires AI models to excel at three hard tasks at once: understanding natural language, structuring abstract math, and executing precise computations\nIn recent years, large language models (LLMs) have demonstrated the ability to write code, solve mathematical problems, and even approach human-expert levels in some highly difficult reasoning tasks. However, a counter-intuitive phenomenon exists: when faced with classic modeling tasks in operations research, their performance degrades rapidly as problem complexity increases. To determine whether this phenomenon stems from a lack of precise computational ability or a failure to truly understand complex constraints and modeling logic, a recent research work introduced an extensible benchmark framework called OPT-Engine. Based on OPT-Engine, the research team focused on three core questions:\n\nIs pure-text reasoning sufficient? The team explored whether the pure-text reasoning (PTR) paradigm, based on classic mathematical chain-of-thought logic, can effectively handle optimization tasks. Experiments show that as task complexity increases, PTR experiences a severe robustness gap. The “effectiveness” of many current approaches largely stems from the sheer simplicity of existing benchmark tasks.\nIs text chain-of-thought reasoning combined with computational tools enough? The researchers tested if introducing external calculation tools (like SciPy) could compensate for PTR’s arithmetic weaknesses. Results indicate that while tools assist with localized calculations, they still struggle to resolve global optimization constraints and overarching modeling logic.\nWhere is the bottleneck of current mainstream paradigms? The current state-of-the-art (SOTA) paradigm—Solver-Integrated Reasoning (SIR)—outperforms pure-text reasoning but remains unsatisfactory on relatively complex optimization problems. Through controlled experiments, the team pinpointed the core bottleneck: it lies in whether LLMs can accurately understand natural language constraints and automatically translate them into precise mathematical models.\n\nThis research systematically evaluates the true performance of mainstream LLM reasoning paradigms on optimization modeling tasks. It reveals the key shortcoming of existing SOTA models in complex decision-making scenarios: the real difficulty is not “calculating the answer,” but correctly understanding and modeling constraints. This points out a clear direction for the future development of AI in operations research modeling.\nThe related research has been accepted by ICML 2026. The code and data are open-sourced on GitHub at https://github.com/Cardinal-Operations/OPTEngine. The paper is available at https://arxiv.org/abs/2601.19924."
  },
  {
    "objectID": "posts/02_optengine.html#opt-engine-why-do-llms-need-a-new-testing-ground-for-optimization-modeling",
    "href": "posts/02_optengine.html#opt-engine-why-do-llms-need-a-new-testing-ground-for-optimization-modeling",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "Before OPT-Engine, many papers claimed LLMs achieved SOTA results on optimization tasks, but an undeniable fact remained: existing test questions were often too simple. They primarily consisted of fixed-scale, small “textbook” problems, such as the classic Traveling Salesperson Problem (TSP) that only required planning routes for 5 nodes. These tasks verify if AI is familiar with standard problem types, but they fail to simulate complex commercial environments involving thousands of variables. Conversely, feeding real-world industrial problems directly to LLMs—which could span millions of tokens in natural language—is neither realistic nor economical.\nTo address this, the research team proposed a systematic compromise based on the complexity of existing benchmarks. Inspired by LeetCode’s scalable complexity concept, OPT-Engine allows problem difficulty to scale slowly and controllably. This design allows researchers to track the trajectory of model capabilities as complexity grows, rather than just yielding a static score at a fixed difficulty level. For example, the number of cities in a TSP can be gradually expanded from 5 to 100, striking an ideal balance between the practical feasibility of text descriptions and the need for rigorous model capability evaluation.\nThe framework covers 10 classic operations research problem types across two major categories:\n\nLinear Programming (LP): This includes inventory management and portfolio optimization problems.\nMixed Integer Programming (MIP): This includes the Traveling Salesperson Problem (TSP) and the knapsack problem."
  },
  {
    "objectID": "posts/02_optengine.html#pure-text-reasoning-ptr-vs.-solver-integrated-reasoning-sir",
    "href": "posts/02_optengine.html#pure-text-reasoning-ptr-vs.-solver-integrated-reasoning-sir",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "There are currently two representative routes in research on LLMs solving optimization problems.\nThe first is Pure-Text Reasoning (PTR), which naturally extends the classic mathematical reasoning chain-of-thought paradigm to optimization tasks. It does not rely on external tools and prompts the LLM to reason step-by-step to generate final results. Its advantage is being concise, end-to-end, and tool-free, with a typical example being Google’s paper Large Language Models as Optimizers.\nThe second route is Solver-Integrated Reasoning (SIR), which is the more mainstream approach for solving optimization tasks today. Examples include agentic methods like OptiMUS or fine-tuning methods like ORLM, SIRL, and OptMATH. The core idea is that the LLM understands the problem and generates the mathematical model or solver code, while the heavy numerical calculations are offloaded to professional external optimization solvers like Gurobi or COPT.\nOn simple problems, such as mainstream benchmarks prior to 2025, PTR and SIR perform comparably. However, system evaluations by the research team using top-tier LLMs from early 2026 within the OPT-Engine framework revealed that PTR’s accuracy experiences a cliff-like drop once the problem scale increases. In contrast, SIR demonstrates stronger stability at higher complexities by offloading exact calculations to external solvers.\nThis comparison yields two key conclusions:\n\nThe Performance Ceiling of PTR: Without external calculation tools, PTR quickly hits its ceiling as complexity rises. This degradation highlights a fundamental mismatch between the text reasoning capabilities LLMs excel at and the precise algorithmic calculations required by optimization problems.\nThe Paradigm Advantage of SIR: By generating executable solver code and calling tool interfaces, LLMs consistently outperform tool-free pure-text solutions. This “task offloading” strategy shifts the precise computational burden to deterministic solvers, allowing the model to maintain strong robustness in complexity ranges where PTR has already failed. This proves that in complex optimization tasks, LLMs are better suited to play the role of “modeling assistants” or “tool coordinators” rather than completely replacing the optimization algorithms themselves.\n\nThe study explicitly concludes that SIR is the necessary framework for tackling high-complexity, industrial-grade optimization challenges moving forward."
  },
  {
    "objectID": "posts/02_optengine.html#can-equipping-llms-with-a-calculator-compptr-solve-the-problem",
    "href": "posts/02_optengine.html#can-equipping-llms-with-a-calculator-compptr-solve-the-problem",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "Since the performance of chain-of-thought-based PTR degrades significantly as problem scale increases due to precision demands, it is natural to wonder if providing external calculation tools would solve the issue. To investigate whether a “calculator” fixes PTR’s flaws, the research team designed a rigorous three-stage system analysis and experiment:\n\nTrajectory and Pattern Analysis: Using TSP as a case study, the team analyzed token-level response patterns of LLM reply trajectories under the PTR mode, combining this with full reasoning trajectories for case studies.\nError Decomposition Experiment: Errors in optimization tasks were explicitly divided into constraint infeasibility errors and other errors (like imprecise calculations). By defining “infeasibility” as violating the feasible region, they decoupled structural modeling failures from arithmetic noise, enabling strict categorization of PTR errors.\nCompPTR Paradigm Design: CompPTR acts as an enhanced version of PTR where the model reasons primarily via natural language but is permitted to use Python tools for auxiliary calculations. It provides the model a “calculator” without allowing it to hand the entire problem over to a professional solver. This helps answer whether external tools improve global feasibility or only patch local arithmetic weaknesses while underlying global logical flaws remain unresolved.\n\n\n\nBased on a fine-grained analysis of DeepSeek-V3.2’s output behavior on the TSP problem, the research team mapped the relationship between average output length (tokens) and accuracy:\n\nSmall-Scale Phase: The model proportionally increases its “reasoning effort” as problem complexity rises, with output tokens growing roughly linearly. Because the search space (a scale of (n-1)!) remains manageable, the model relies on explicitly enumerating all Hamiltonian cycles for precise solving, resulting in high accuracy.\nPost-Critical Threshold Phase: When the number of cities nears a critical point right before a cliff-like drop in accuracy, the model exhibits counter-intuitive behavior: token budgets actually decrease despite the increased task difficulty.\n\nA distinct strategy shift is visible: moving from precise solving to heuristic estimation.\n\nAfter crossing the threshold, the model actively abandons exact enumeration and switches to lightweight heuristic methods like Nearest Neighbor and Cheapest Insertion to obtain approximate solutions. Token consumption levels off, and solution quality subsequently drops.\nThis adaptive behavior somewhat mimics human expert practices: when precise solving becomes computationally unfeasible, the model swiftly switches to rapid heuristic algorithms.\n\nThis dynamic mechanism explains the reported effectiveness of previous PTR-based end-to-end optimizers, providing a mechanistic explanation for their success. Their success stems not from truly “precisely solving” complex problems, but because the model internally learned to dynamically switch to heuristic methods, trading optimal solutions for sub-optimal but practical approximations with acceptable evaluation metrics. Essentially, they score well because the evaluation standard implicitly shifts to an “open-book, merely passing” criteria.\n\n\n\nTo strictly verify the observation that PTR behaves more like a stochastic approximation, the team categorized errors into two distinct types:\n\nInfeasibility Errors: Violations of constraint conditions, indicating a structural modeling failure.\nOther Errors: Errors primarily stemming from calculation inaccuracies, such as arithmetic mistakes.\n\nThe comparison revealed completely different failure modes:\n\nFor the SIR Paradigm: Accuracy is strictly governed by structural feasibility. Because numerical calculations are fully offloaded to external solvers, SIR’s bottleneck is purely structural. As long as the model provides the correct logic mapping (constraints and objective functions are error-free), the solver guarantees optimality. If the structure is right, the result is right.\nFor the PTR Paradigm: PTR exhibits a dual-failure trajectory. As complexity increases, PTR fails in both feasibility and computational precision. First, it suffers a “reasoning collapse” where it cannot satisfy global logic constraints, causing feasibility rates to plummet. Second, it suffers from “insufficient precision,” meaning even if the model accurately understands constraints, it lacks the deterministic numerical precision required for an exact optimal solution.\n\nIn short, SIR’s failure is “one-dimensional” (wrong only if the structure is wrong), while PTR’s failure is “two-dimensional” (the structure can be wrong, and the numbers are hard to get precise).\n\n\n\nThe CompPTR framework permits the model to use Python-based computing primitives (like scipy or itertools) for auxiliary numerical calculations, but it explicitly forbids the model from utilizing external optimization solvers (like Gurobi or COPT) for global optimization. Experiments show that CompPTR’s performance sits in the middle—better than PTR, but failing to reach the heights of SIR.\nObservations of CompPTR highlight the following:\n\nSignificant Improvements in Feasibility and Precision: External computational support not only mitigates PTR’s inherent arithmetic errors but also reduces infeasibility errors previously caused by reasoning failures.\nInability to Match SIR: CompPTR still encounters reasoning collapse as complexity scales. While it successfully uses tools for discrete subtasks, it fails to internalize the global optimality requirements of optimization problems. It struggles to maintain a coherent global optimization strategy, leading to continued performance degradation as problem scales grow.\n\nUltimately, computational tools can fix localized issues, but they cannot cure global structural deficiencies. Local arithmetic precision is useless without a global modeling perspective in complex optimization tasks."
  },
  {
    "objectID": "posts/02_optengine.html#bottleneck-pinpointing-llms-memorize-rather-than-solve",
    "href": "posts/02_optengine.html#bottleneck-pinpointing-llms-memorize-rather-than-solve",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "If SIR is the reliable framework for large-scale industrial problems, why do LLMs—which perform almost perfectly on human intellectual math tasks like the Math Olympiad—still underperform on textbook-level operations research problems? To locate the true bottleneck, the research team designed three controlled experiments:\n\n\n\n\n\n\n\n\nExperiment Type\nMethodology\nConclusion\n\n\n\n\nLinguistic Complexity Control\nBuilt derivative versions of descriptive templates with increasing syntactic and vocabulary complexity using Perplexity (PPL) as a metric, while keeping the underlying numerical instances and constraint sets identical.\n\n\n\n\n| Model accuracy does not significantly drop as language complexity (PPL) increases. The model’s failure is not due to an “inability to read complex expressions.”\n| | Objective Function Perturbation | Applied linear perturbations (like adding a constant term) to the original objective function to alter the optimization criteria.\n| Model performance is almost entirely unaffected. Objective function variations are not the primary bottleneck.\n| | Constraint Enhancement | Added mathematically simple but semantically demanding constraints, such as excluding a specific edge in TSP, adding mutual exclusion in the knapsack problem, or setting order caps in inventory management. These are non-binding or trivial constraints that do not change asymptotic complexity, only the semantic modeling burden.\n| Accuracy drops significantly for both the SIR and PTR paradigms.\n|\nCore Interpretation: This counter-intuitive pattern indicates that current LLMs may not have truly internalized or robustly reasoned through problem constraints. Instead, they heavily rely on standard textbook templates (like classic TSP and knapsack problems) that are abundant in their training data. Standard versions closely match their training data, but constraint-enhanced versions introduce condition combinations the models are unlikely to have seen.\nReal-world optimization tasks rarely appear in “clean” standard forms and are almost always accompanied by customized operational or regulatory constraints. This reveals an awkward truth: currently, LLMs are largely “memorizing” problems. They excel at familiar standard types but fail to handle slightly modified, customized constraints. The automated modeling of constraints is currently the biggest bottleneck in AI-driven operations research."
  },
  {
    "objectID": "posts/02_optengine.html#summary-and-outlook",
    "href": "posts/02_optengine.html#summary-and-outlook",
    "title": "OPT-Engine: Benchmarking the Limits of LLMs in Optimization Modeling via Complexity Scaling",
    "section": "",
    "text": "OPT-Engine is a highly inspiring piece of work in the field of LLMs solving optimization problems. It builds a quantifiable and scalable bridge between textbook “toy examples” and complex real-world industrial problems. By leveraging classic LP and MIP problems and adapting LeetCode’s complexity scaling, it allows optimization difficulty to rise slowly and controllably to scientifically evaluate the true performance of different reasoning paradigms.\nThe core conclusions of the study are summarized in three main points:\n\nThe Golden Paradigm for Industrial Application: At the current stage, “LLM Brain + Professional Solver Execution” (SIR) is the most capable paradigm capable of gaining a foothold in real industrial scenarios.\nThe Fatal Bottleneck of SIR: Even SOTA SIR systems have inherent flaws in understanding natural language descriptions of complex problems and logically translating complex, non-standard constraints.\nViable Future Directions: A promising improvement for solving complex problems is the Meta-Solver. This approach aims to make LLMs act like human experts, automatically selecting the most appropriate solving strategy based on prior experience for different problems (e.g., automatically switching to heuristic algorithms for multi-node TSPs).\n\nThe future direction worth exploring is not forcing LLMs to directly “replace solvers,” but rather enabling them to become more reliable optimization modeling and tool-coordination systems. They need to understand problem structures, identify constraint types, select suitable algorithms, and call upon professional solvers or heuristics when necessary. In this sense, OPT-Engine not only diagnoses the current boundaries of LLMs in optimization modeling, but it also points the way forward for the next generation of AI optimization systems: moving from “solving standard problems” to “understanding real constraints,” and transitioning from “directly giving answers” to “intelligent modeling and tool synergy”.\n\n\nInformation theory (Shannon 1948) provides a rigorous mathematical framework for quantifying “information” and “uncertainty.”\nEvery event carries a certain amount of information. For a random event, its information content can be intuitively understood as the “degree of surprise”: the lower the prior probability of an event, the more information it conveys when it actually occurs. Take weather as an example: * “It rained yesterday” is an established fact with zero surprise, so it contains zero information. * However, “it will rain tomorrow” remains uncertain, so when it happens, it carries actual information.\nImagine you are hiking deep in the mountains with absolutely no cell phone signal. You know nothing about tomorrow’s cloud cover, humidity, atmospheric pressure, or any other physical conditions. In such a state of extreme cognitive deprivation, the most conservative inference you can make, is that that the probabilities of tomorrow being sunny, cloudy, or rainy are evenly split at 1/3 each, based on the principle of symmetry. This reflects a severe lack of knowledge regarding the weather system’s underlying laws—to you, the system’s uncertainty is immense.\nIn contrast, if you check an authoritative weather forecast today and learn that the probability of a sunny day tomorrow is 0.99. Where does such high certainty come from? In fact, a massive amount of information has already been injected into the forecasting system in advance. Meteorologists have digested the original uncertainty using satellite images, historical weather models, and supercomputer simulations. Achieving such high certainty means that, during this prediction process, physical determinism has overwhelmingly outweighed randomness. From this we can draw a rather philosophical conclusion:\nInformation is essentially the reduction in uncertainty from learning an outcome.\nTo quantify how much uncertainty has been reduced, we need a metrics to measure the uncertainty. This leads to the mathematical definition of information. For a random variable (X) defined on a discrete outcome space () with probability mass function (p(x)), the amount of information gained by observing the event (X=x), the self-information,is defined as:\n\\[\nI(X=x) = -\\log p(x)\n\\]\nThe base of the logarithm is usuall 2 (unit: bit) or base (e) (unit: nat). (I(x)) measures the “amount of information” obtained when we observe a specific outcome (x). Returning to the weather example:\n\nExample 1: If “it rained yesterday” is a certain fact, \\(p(x=\\text{rainy}) = 1\\), then \\(I(x) = -\\ln(1) = 0\\). A certain event contains no information.\nExample 2: Suppose a weather station predicts that the prior probability of rain tomorrow is \\(p(x=\\text{rainy}) = 0.1\\). Then the self-information of the event “it rains tomorrow” is \\(I(X=\\text{rainy}) = -\\ln(0.1) \\approx 2.3\\) nats. This means that if it actually rains tomorrow, we will receive about 2.3 nats of “information” or “surprise.”\n\n\n\n\nUsing the weather example, if the probability of rain is \\(p(x)\\), the information content of the event “rain tomorrow” is \\(-\\log(p(x))\\). If we enumerate all possible outcomes (rainy, cloudy, sunny) and compute the expectation of their information contents. This expectation is what we call entropy.\nMathematically, suppose an event (X) (such as tomorrow’s weather) has \\(n\\) possible outcomes with respective probabilities \\(p(x_i)\\). The expected information, or entropy, is expressed as:\n\\[\nH(X) = \\mathbb{E}_{x \\sim p}[I(x)] = -\\sum_{i=1}^{n} p(x_i) \\log p(x_i) \\quad \\text{(Discrete case)}\n\\]\nThe higher the entropy, the greater the system’s uncertainty. For instance, if the probabilities of tomorrow’s weather are: \\[p(x_1=\\text{rainy}) = 0.1,\\; p(x_2=\\text{cloudy}) = 0.3,\\; p(x_3=\\text{sunny}) = 0.6\\.\\]\nThe entropy is: \\[\nH(X) = -0.1 \\ln(0.1) - 0.3 \\ln(0.3) - 0.6 \\ln(0.6) \\approx 0.898 \\text{ nats}\n\\]\nThis value (approximate 0.898 nats) represents our measure of the average uncertainty regarding tomorrow’s weather.\n\n\n\nIn reality, we rarely know the true distribution \\(p\\); instead, we have a predicted distribution \\(q\\). For instance, we might rely on a weather app to predict tomorrow’s weather, but that prediction isn’t necessarily perfect. We can use cross-entropy \\(H(p, q)\\) to define the measurement cost required when we use a suboptimal predicted distribution \\(q\\) to represent the average information from the true distribution \\(p\\): \\[\nH(p, q) = \\mathbb{E}_{x \\sim p(x)}[-\\log q(x)] = -\\sum_{x} p(x) \\log q(x)\n\\] Note that the expectation is taken with respect to the true distribution \\(p\\), but the log probabilities come from the predictive model distribution \\(q\\).\nLet’s go back to the weather forecast example we looked at earlier. Suppose the true probabilities of sunny, cloudy, and rainy are \\(p = \\{0.1, 0.3, 0.6\\}\\). First, we calculate the entropy of the true distribution itself, which is the minimum average information required under optimal encoding (using base \\(e\\), unit: nats):\n\\[\nH(p) = -\\sum_{x} p(x) \\log p(x) = -(0.1\\ln0.1 + 0.3\\ln0.3 + 0.6\\ln0.6) \\approx 0.898\n\\] Now, suppose two weather apps provide different predicted distributions for tomorrow:\n\nModel A (More accurate): \\(q_A = \\{0.15, 0.25, 0.60\\}\\)\nModel B (Less accurate): \\(q_B = \\{0.70, 0.20, 0.10\\}\\)\n\nWe can calculate the cross-entropy of these two predictions relative to the true distribution:\n\nCross-entropy of Model A: \\(H(p,q_A) = -(0.1\\ln0.15 + 0.3\\ln0.25 + 0.6\\ln0.60) \\approx 0.912\\)\nCross-entropy of Model B: \\(H(p,q_B) = -(0.1\\ln0.70 + 0.3\\ln0.20 + 0.6\\ln0.10) \\approx 1.901\\)\n\nAs we can see, Model A’s cross-entropy (0.912) is much closer to the true entropy (0.897). A lower cross-entropy indicates that the predicted distribution is closer to the true distribution.\nBy generalizing the weather classification example, we arrive at the cross-entropy loss function—the workhorse behind most machine learning classifiers. When we train deep neural networks for tasks like image classification or language modeling, the central goal is to minimize this cross-entropy loss via backpropagation. In doing so, we drive the model’s predicted distribution qq to match the true label distribution pp as closely as possible.\n\n\n\nIn the weather prediction example above, a good prediction model (Model A) is clearly superior to a poor one (Model B). But what if we want to further quantify exactly how much extra information cost (i.e., cognitive bias or prediction error) we paid because the predicted distribution \\(q\\) is inaccurate compared to the ideal state \\(p\\)? This is exactly what KL Divergence answers (Kullback and Leibler 1951).\nIn information theory, there is a classic information cost decomposition:\n\\[\n\\underbrace{H(p, q)}_{\\text{Total predictive cost}} = \\underbrace{H(p)}_{\\text{Inherent randomness (minimum possible cost)}} + \\underbrace{D_{\\mathrm{KL}}(p \\| q)}_{\\text{Extra cost due to model inaccuracy}}\n\\]\nIntuitively:\n\n\\(H(p)\\) (entropy): this is the intrinsic physical noise of the world. Even if your model were omniscient, this part of uncertainty could never be eliminated.\n\\(D_{\\mathrm{KL}}(p \\| q)\\) (KL divergence): this is the “cognitive gap” between you and the real world. It measures the “distance” (asymmetric) between two probability distributions. the information redundancy or extra waste caused by the model’s imperfection.\n\nWe can then derive the formula of KL divergence:\n\\[\nD_{\\mathrm{KL}}(p \\| q) = H(p, q) - H(p) = \\sum_{i=1}^{n} p(x_i) \\log\\left(\\frac{p(x_i)}{q(x_i)}\\right)\n\\]\nUsing our running example, the true distribution \\(p=\\{0.1, 0.3, 0.6\\}\\) has an entropy \\(H(p) \\approx 0.897\\) nats.\nModel A (More accurate):\n\nPrediction: \\(q_A = \\{0.15, 0.25, 0.60\\}\\)\nCross-Entropy: \\(H(p, q_A) \\approx 0.912\\) nats\nKL Divergence: \\(D_{KL}(p \\parallel q_A) = 0.912 - 0.897 = 0.015\\) nats\nAnalysis: Information redundancy is extremely small (near 0), indicating Model A’s predictive efficiency is very close to the theoretical optimum.\n\nModel B (Less accurate):\n\nPrediction: \\(q_B = \\{0.70, 0.20, 0.10\\}\\)\nCross-Entropy: \\(H(p, q_B) \\approx 1.901\\) nats\nKL Divergence: \\(D_{KL}(p \\parallel q_B) = 1.901 - 0.897 = 1.004\\) nats\nAnalysis: Huge information redundancy. This means due to Model B’s severe bias, every time it encodes weather information, it wastes over 1 nat of information just dealing with its own errors.\n\nKL Divergence has two classic properties:\n\n: \\(D_{KL}(p \\parallel q) \\neq D_{KL}(q \\parallel p)\\). KL divergence is not a true geometric “distance metric” (which requires symmetry). It measures the information lost when using \\(q\\) to approximate \\(p\\), taking \\(p\\) as the baseline.\nNon-negativity: \\(D_{KL}(p \\parallel q) \\ge 0\\). According to Jensen’s inequality, the divergence is 0 if and only if \\(p=q\\). This reflects a common-sense principle in information theory: no predictive encoding can be better than the true distribution itself. Any imperfect model will inevitably incur extra information costs.\n\n\n\n\nThe principles of entropy, cross-entropy, and KL divergence form a powerful “information-cost” framework that naturally extends into the core of modern machine learning, deep learning, and reinforcement learning.\nSupervised Learning: In classic single-label classification, the ground-truth label is typically represented as a one-hot vector. For the correct class \\(y^*\\), the true probability is \\(P(y^*|x) = 1\\) and zero for all other classes, resulting in a true distribution entropy of \\(H(P) = 0\\). Consequently, the cross-entropy loss used to train classifiers reduces entirely to the negative log-likelihood:\n\\[\n\\mathcal{L}_{\\mathrm{CE}} = H(P, Q) = -\\sum_{y} P(y|x) \\log Q(y|x) = -\\log Q(y^*|x).\n\\]\nBecause the inherent entropy \\(H(P) = 0\\), minimizing the cross-entropy is mathematically equivalent to minimizing the KL divergence \\(D_{\\mathrm{KL}}(P \\parallel Q)\\)—essentially forcing the model’s predicted distribution \\(Q\\) to map perfectly to the true distribution \\(P\\).\nGenerative Models: In generative models like Variational Autoencoders (VAEs) (Kingma and Welling 2013), KL divergence acts directly as an optimization objective to regularize the latent variable distribution. VAEs are trained by maximizing the Evidence Lower Bound (ELBO), formulated as: \\[\n\\mathrm{ELBO} = \\mathbb{E}_{z \\sim Q_\\phi(z|x)}[\\log P_\\theta(x|z)] - \\beta \\cdot D_{\\mathrm{KL}}(Q_\\phi(z|x) \\| P(z)).\n\\] The corresponding minimization loss is (_{} = -).\nTo reframe this as a minimization problem, the loss function becomes \\(\\mathcal{L}_{\\mathrm{VAE}} = -\\mathrm{ELBO}\\), which balances two critical components:\n\nThe Reconstruction Term: \\(\\mathbb{E}[\\log P_\\theta(x|z)]\\) ensures the decoder can accurately reconstruct the real data \\(x\\) from the latent variable \\(z\\) (typically measured via mean squared error or cross-entropy).\nThe Regularization Term: \\(D_{\\mathrm{KL}}(Q_\\phi(z|x) \\parallel P(z))\\) forces the encoder’s output posterior distribution \\(Q_\\phi(z|x)\\) to approximate a defined prior \\(P(z)\\) (such as a standard normal distribution \\(\\mathcal{N}(0, I)\\)).\n\nKnowledge Distillation:  Knowledge distillation(Hinton et al. 2015) aims to transfer the rich representations embedded in a large “teacher model” (such as DeepSeek, GPT, or Gemini) into a lightweight “student model.” To ensure the student learns not only the hard labels (0/1) but also the nuanced inter-class similarities discovered by the teacher, we introduce KL divergence to align their soft labels:\n\\[\n\\mathcal{L}_{\\mathrm{KD}} = \\alpha \\cdot \\mathcal{L}_{\\mathrm{CE}}(y, q_s) + (1-\\alpha) \\cdot T^2 \\cdot D_{\\mathrm{KL}}(p_t \\| p_s),\n\\] where the teacher’s and student’s smoothed probabilities at temperature \\(T\\) are defined as: \\[\np_t = \\mathrm{softmax}\\!\\left(\\frac{z_t}{T}\\right), \\quad p_s = \\mathrm{softmax}\\!\\left(\\frac{z_s}{T}\\right).\n\\]\nA higher temperature \\(T\\) yields a smoother distribution that reveals hidden inter-class relationships, while multiplying the term by \\(T^2\\) preserves the original gradient scale of the soft-label loss. By minimizing \\(D_{\\mathrm{KL}}(p_t \\parallel p_s)\\), the student model is forced to replicate the teacher’s reasoning logic and predictive confidence. This yields a significantly stronger generalization capability than could be achieved by fitting hard labels alone.\nReinforcement Learning:  In reinforcement learning (RL), KL divergence and entropy play distinct but equally critical roles in stabilizing training and promoting exploration.\n\nKL divergence for constraining policy updates: In the clipped version of Proximal Policy Optimization (PPO) (Schulman et al. 2017), the objective function is: \\[\n  \\mathcal{L}^{\\mathrm{CLIP}}(\\theta) = \\hat{\\mathbb{E}}_t \\left[ \\min\\left( r_t(\\theta) \\hat{A}_t,\\; \\mathrm{clip}(r_t(\\theta), 1-\\epsilon, 1+\\epsilon) \\hat{A}_t \\right) \\right],\n  \\] where \\(r_t(\\theta) = \\frac{\\pi_\\theta(a_t|s_t)}{\\pi_{\\theta_{\\mathrm{old}}}(a_t|s_t)}\\). By restricting the probability ratio \\(r_t(\\theta)\\) to the interval \\([1-\\epsilon, 1+\\epsilon]\\), the clipping operation implicitly bounds the KL divergence \\(D_{\\mathrm{KL}}(\\pi_{\\theta_{\\mathrm{old}}} \\parallel \\pi_\\theta)\\). This prevents drastic, destructive policy updates and ensures monotonic training stability.\nEntropy for encouraging exploration: To prevent a policy from prematurely converging to a deterministic, suboptimal solution, many policy gradient methods incorporate an entropy regularization term into the loss:\n\\[\n  \\mathcal{L} = \\mathbb{E}_t\\left[ \\log \\pi_\\theta(a_t|s_t) \\hat{A}_t \\right] + \\beta \\cdot H(\\pi_\\theta(\\cdot|s_t)),\n\\]\n\nSoft Actor-Critic (SAC) (Haarnoja et al. 2018) advances this concept by establishing a formal “maximum entropy” RL framework. SAC maximizes a weighted sum of the expected return and the policy’s entropy by optimizing the following objective:\\[J(\\pi) = \\sum_t \\mathbb{E}_{(s_t, a_t)} \\left[ r(s_t, a_t) + \\alpha H(\\pi(\\cdot|s_t)) \\right].\\]This formulation structurally guarantees a superior balance between exploration and exploitation, leading to more robust policies with enhanced generalization across varied environments.\n\n\n\nBefore wrapping up, let us revisit the origins of entropy and KL divergence in their birthplace: thermodynamics and statistical physics.\nThe Physical picture: particles and energy levels:  Imagine randomly throwing \\(N\\) small stones (analogous to independent particles) into \\(M\\) buckets (analogous to energy levels). The bucket each stone lands in is random, ultimately yielding an observed distribution where \\(n_i\\) is the number of stones in bucket \\(i\\). This process naturally generates a probability distribution where the proportion of stones in each bucket is \\(p_i = n_i / N\\).\nSuppose we only care about the final macrostate—how many stones end up in each bucket, regardless of which specific stone went where. We describe this macrostate using a vector \\(\\mathbf{n} = \\{n_1, n_2, \\dots, n_M\\}\\) under the strict constraint that \\(\\sum n_i = N\\). The question then becomes: How many microscopic arrangements (microstates) lead to this exact macrostate?\nThe first stone has \\(N\\) possible placements, the second \\(N-1\\), and so on, giving \\(N!\\) total permutations. However, because we do not care about the ordering of stones within the same bucket, we must divide out the redundant permutations by taking the factorial of each bucket’s occupation number. This gives us the total number of microstates (the multinomial coefficient) behind the macrostate:\n\\[W = \\frac{N!}{\\prod_{i=1}^{M} n_i!}\\]\nIn physics, \\(W\\) is known as the multiplicity. Ludwig Boltzmann famously proposed that the physical entropy of a system is proportional to the logarithm of its multiplicity. Therefore, we can define the physical entropy per particle as:\nIf we further assume that each bucket has an equal prior probability (i.e., a uniform distribution with probability (1/M)), then the total probability of observing the macrostate () follows a standard multinomial distribution:\n\\[H = \\frac{1}{N} \\log W\\]\nIf we assume each bucket has an equal prior probability of catching a stone (a uniform distribution with probability \\(1/M\\)), the total probability of observing the macrostate \\(\\mathbf{n}\\) follows a standard multinomial distribution:\n\\[P(n_1, n_2, \\dots, n_M) = W \\cdot \\left(\\frac{1}{M}\\right)^N\\]\nTaking the logarithm of this probability and dividing by \\(N\\), we obtain:\n\\[\\frac{1}{N} \\log P = \\frac{1}{N} \\log W + \\log\\!\\left(\\frac{1}{M}\\right)\\]\nBecause the second term is a fixed constant, we uncover a profound physical truth: finding the macrostate with the largest multiplicity \\(W\\) is mathematically equivalent to finding the state with the highest probability of occurring.\nUniform distribution to Shannon entropy:  Now, let’s explore what happens in the macroscopic limit as \\(N \\to \\infty\\), while keeping the bucket ratios \\(p_i = n_i/N\\) fixed. Expanding our entropy equation \\(H\\):\n\\[H = \\frac{1}{N} \\log(N!) - \\frac{1}{N} \\sum_{i=1}^{M} \\log(n_i!)\\]\nBy applying Stirling’s approximation (\\(\\log(x!) \\approx x \\log x - x\\)) for large numbers, the equation transforms into:\n\\[\nH \\approx \\frac{1}{N} \\left( N \\log N - N - \\sum_{i=1}^{M} (n_i \\log n_i - n_i) \\right)\n\\]\nGiven our constraint that \\(\\sum n_i = N\\), the linear \\(N\\) terms cancel out, simplifying the expression to:\n\\[\nH \\approx \\log N - \\frac{1}{N} \\sum_{i=1}^{M} n_i \\log n_i\n\\]\nNext, we substitute \\(n_i = N p_i\\) back into the sum:\n\\[\\begin{aligned}\nH &\\approx \\log N - \\frac{1}{N} \\sum_{i=1}^{M} (N p_i \\log (N p_i)) \\\\\n&= \\log N - \\sum_{i=1}^{M} p_i (\\log N + \\log p_i) \\\\\n&= \\log N - (\\log N) \\sum_{i=1}^{M} p_i - \\sum_{i=1}^{M} p_i \\log p_i\n\\end{aligned}\\]\nBecause the probabilities must sum to one (\\(\\sum p_i = 1\\)), the \\(\\log N\\) terms beautifully cancel each other out. Out of a classical physics thought experiment, we arrive directly at the formula for Shannon Information Entropy:\n\\[H = -\\sum_{i=1}^{M} p_i \\log p_i\\]\nFrom Non-uniform distribution to KL divergence: Now consider a twist: what if the inherent probabilities of a stone falling into each bucket are not equal? What if they follow a non-uniform prior distribution \\(q = \\{q_1, q_2, \\dots, q_M\\}\\) dictated by natural laws or environmental biases? In this scenario, the total probability of observing the macro-event becomes:\n\\[\nP(n_1, n_2, \\dots, n_M) = W \\prod_{i=1}^{M} (q_i)^{n_i}\n\\]\nIf we take the logarithm of this new non-uniform probability, divide by \\(N\\), and apply the exact same Stirling approximation logic as before, the derivation yields:\n\\[\n\\frac{1}{N} \\log P(n_1, n_2, \\dots, n_M) \\approx -\\sum_{i=1}^{M} p_i \\log\\left(\\frac{p_i}{q_i}\\right)\n\\] The right-hand side is instantly recognizable—it is precisely the negative Kullback-Leibler (KL) divergence, \\(-D_{\\mathrm{KL}}(p \\parallel q)\\).\nWe recognize the right-hand side as exactly the negative of the KL divergence, (-D_{}(p | q)).\nThe Takeaway: In the macroscopic limit, the logarithm of the probability of observing a specific macrostate is exactly the negative KL divergence between the observed distribution \\(p\\) and the prior distribution \\(q\\).\nBringing It Back to Machine Learning: This brings our story full circle. When we train a machine learning model by minimizing \\(D_{\\mathrm{KL}}(p \\parallel q)\\), the underlying physical interpretation is identical. By optimizing the model parameters \\(q\\), we are actively transforming the probability of observing our training data \\(p\\) from an “extremely unlikely, rare outlier” into the “most natural, highest-probability typical event.”\nThis foundational logic governs variational inference, knowledge distillation, and policy constraints in reinforcement learning. It also explains why physicists without formal AI backgrounds often make rapid, groundbreaking contributions to machine learning—beneath the code and the compute, the two domains speak the exact same language at their core.\n\n\n\nSince C. E. Shannon proposed the mathematical formulation of information entropy in 1948, this theoretical framework for quantifying uncertainty has laid the bedrock for information theory and statistical learning, continuously shaping the landscape of modern AI and ML.\nFrom the basic cognitive bias in weather forecasting to the cross-entropy loss that guides parameter updates in deep learning; from probability approximations in VAEs and distribution matching in knowledge distillation, to KL divergence regularization used to constrain LLMs(Ouyang et al. 2022) and RL training—the “information cost” metric system built on entropy, cross-entropy, and KL divergence is more than just the underlying grammar of ML algorithms. It has become our core analytical tool for understanding and navigating a world of uncertainty.\nMastering this framework not only clarifies “what is being calculated” during model optimization, but more importantly, uncovers the fundamental logic of “why it is calculated this way.”"
  },
  {
    "objectID": "posts/02_optharness.html",
    "href": "posts/02_optharness.html",
    "title": "Harness Engineering for Optimization Modeling: Strategy, Implementation, and Expansion",
    "section": "",
    "text": "The NeurIPS 2025 paper, Solver-Informed RL: Grounding Large Language Models for Authentic Optimization Modeling (Chen et al. 2026), presents a pioneering approach to applying Reinforcement Learning with Verifiable Rewards (RLVR) to train LLMs for optimization tasks. Its core contribution is Solver-Informed Reinforcement Learning(SIRL), a tailed RLVR framework that incorporates optimization solvers into the model-training loop.\nBy interacting with external solvers such as Gurobi and COPT, the SIRL framework converts generated optimization code into concrete execution signals, including solver syntax status, infeasibility indicators, objective values, and decision-variable assignments. These signals are then transformed into structured and verifiable rewards. Instead of evaluating generated code solely on syntax or similarity to reference solutions, the framework assesses whether the resulting mathematical model is valid, feasible, and capable of producing the intended optimization solution. In this process, the solver acts as a strict and objective referee. Its feedback helps iteratively refine the LLM’s policy, directly aligning code generation with mathematical correctness and solver-verified performance. As a result, the model develops greater proficiency in formulating and solving authentic optimization problems.\nIn this post, we look under the hood of the SIRL framework’s engineering backbone: a specialized harness designed for secure code execution and evaluation in LLM-driven optimization modeling. By routing generated code directly into sandboxed optimization solvers, this harness manages code re-expression, execution dispatch, and the critical translation of raw runtime outcomes into the multi-dimensional reward signals essential for the RLVR (Reinforcement Learning with Verifiable Rewards) training loop. Closing the loop between generation and execution empowers LLMs to safely test, evaluate, and iteratively refine their code against real solver dynamics. Ultimately, this framework for secure execution, solver integration, and failure handling is the missing link that allows Solver-Informed RL to graduate from clean academic testbeds to messy, highly constrained real-world business environments.\n\n\nFollowing the paradigms of prompt engineering and context engineering, harness engineering has emerged as a new approach for building reliable AI systems. It gained widespread attention after the release of the OpenAI technical report &lt;&lt;Harness Engineering: Leveraging Codex in an Agent‑First World&gt;&gt; (Lopopolo 2026).\nBecause large language models are built on transformer architectures and autoregressive sampling, their outputs are inherently stochastic. Even the most capable models can give different answers to the same prompt, make inconsistent decisions, or fail unpredictably. Harness Engineering tackles this uncertainty by enveloping the model in a dedicated layer of software infrastructure and control logic. This layer governs how the model receives information, invokes tools, executes actions, validates outputs, handles failures, and incorporates feedback. Its purpose is to anchor a high-entropy generative engine into a controllable, verifiable, and closed-loop system. Through a meticulously designed execution harness, LLMs can be securely encapsulated and deployed into mission-critical business workflows that demand uncompromising stability and auditability.\nA simple way to express this idea is: \\[\nAI\\text{-}Agent = \\underbrace{\\text{SOTA Model}}_{\\text{the wild horse}}\n+ \\underbrace{\\text{Harness}}_{\\text{the control system}}.\n\\]\nThe harness determines whether that capability of current SOTA LLMs can be deployed safely, consistently, and effectively in the real world."
  },
  {
    "objectID": "posts/02_optharness.html#introduction",
    "href": "posts/02_optharness.html#introduction",
    "title": "Harness Engineering for Optimization Modeling: Strategy, Implementation, and Expansion",
    "section": "",
    "text": "The NeurIPS 2025 paper, Solver-Informed RL: Grounding Large Language Models for Authentic Optimization Modeling (Chen et al. 2026), presents a pioneering approach to applying Reinforcement Learning with Verifiable Rewards (RLVR) to train LLMs for optimization tasks. Its core contribution is Solver-Informed Reinforcement Learning(SIRL), a tailed RLVR framework that incorporates optimization solvers into the model-training loop.\nBy interacting with external solvers such as Gurobi and COPT, the SIRL framework converts generated optimization code into concrete execution signals, including solver syntax status, infeasibility indicators, objective values, and decision-variable assignments. These signals are then transformed into structured and verifiable rewards. Instead of evaluating generated code solely on syntax or similarity to reference solutions, the framework assesses whether the resulting mathematical model is valid, feasible, and capable of producing the intended optimization solution. In this process, the solver acts as a strict and objective referee. Its feedback helps iteratively refine the LLM’s policy, directly aligning code generation with mathematical correctness and solver-verified performance. As a result, the model develops greater proficiency in formulating and solving authentic optimization problems.\nIn this post, we look under the hood of the SIRL framework’s engineering backbone: a specialized harness designed for secure code execution and evaluation in LLM-driven optimization modeling. By routing generated code directly into sandboxed optimization solvers, this harness manages code re-expression, execution dispatch, and the critical translation of raw runtime outcomes into the multi-dimensional reward signals essential for the RLVR (Reinforcement Learning with Verifiable Rewards) training loop. Closing the loop between generation and execution empowers LLMs to safely test, evaluate, and iteratively refine their code against real solver dynamics. Ultimately, this framework for secure execution, solver integration, and failure handling is the missing link that allows Solver-Informed RL to graduate from clean academic testbeds to messy, highly constrained real-world business environments.\n\n\nFollowing the paradigms of prompt engineering and context engineering, harness engineering has emerged as a new approach for building reliable AI systems. It gained widespread attention after the release of the OpenAI technical report &lt;&lt;Harness Engineering: Leveraging Codex in an Agent‑First World&gt;&gt; (Lopopolo 2026).\nBecause large language models are built on transformer architectures and autoregressive sampling, their outputs are inherently stochastic. Even the most capable models can give different answers to the same prompt, make inconsistent decisions, or fail unpredictably. Harness Engineering tackles this uncertainty by enveloping the model in a dedicated layer of software infrastructure and control logic. This layer governs how the model receives information, invokes tools, executes actions, validates outputs, handles failures, and incorporates feedback. Its purpose is to anchor a high-entropy generative engine into a controllable, verifiable, and closed-loop system. Through a meticulously designed execution harness, LLMs can be securely encapsulated and deployed into mission-critical business workflows that demand uncompromising stability and auditability.\nA simple way to express this idea is: \\[\nAI\\text{-}Agent = \\underbrace{\\text{SOTA Model}}_{\\text{the wild horse}}\n+ \\underbrace{\\text{Harness}}_{\\text{the control system}}.\n\\]\nThe harness determines whether that capability of current SOTA LLMs can be deployed safely, consistently, and effectively in the real world."
  },
  {
    "objectID": "posts/02_optharness.html#harness-framework-in-the-context-of-optimization-modeling",
    "href": "posts/02_optharness.html#harness-framework-in-the-context-of-optimization-modeling",
    "title": "Harness Engineering for Optimization Modeling: Strategy, Implementation, and Expansion",
    "section": "Harness Framework in the Context of Optimization Modeling",
    "text": "Harness Framework in the Context of Optimization Modeling\nIn a standard Harness Engineering pipeline for code agents, execution is relatively straightforward: the LLM generates a standalone code snippet, which is then executed and validated through a tool call inside an isolated sandbox.\nOptimization solvers (such as Gurobi, COPT, or Google OR‑Tools) have a distinctive characteristic: they demand an object‑oriented workflow. You first initialize a core model instance, then programmatically attach decision variables, constraints, and the objective function, step by step. To see this pattern in action, consider a canonical textbook example—the tire transportation problem (strictly speaking, an integer linear programming problem):\nA company needs to transport at least 200 tires to meet demand. Two modes of transportation are available:\n* Air Transport: Capacity of 10 tires per trip at a cost of $1,000 per trip.\n* Truck Transport: Capacity of 6 tires per trip at a cost of $700 per trip.\n\nConstraints:\n1. Budget Cap: Total transportation cost cannot exceed $22,000.\n2. Trip Ratio: The number of air trips cannot exceed the number of truck trips.\n3. Demand Requirement: Total tires transported $\\ge 200$.\n\nObjective: Determine the optimal number of air and truck trips to minimize the total number of trips while satisfying all constraints.\nBelow is a complete Gurobi script that prints both the optimal objective value and the decision variables.\nfrom gurobipy import GRB\n\nm = gp.Model()\nx = m.addVar(vtype=GRB.INTEGER, name=\"x\")\ny = m.addVar(vtype=GRB.INTEGER, name=\"y\")\n\nm.setObjective(x + y, GRB.MINIMIZE)\nm.addConstr(10*x + 6*y &gt;= 200)\nm.addConstr(1000*x + 700*y &lt;= 22000)\nm.addConstr(x &lt;= y)\n\nm.optimize()\n\nif m.status == GRB.OPTIMAL:\n    print(f\"Just print the best obj: {m.objVal}\")\n    print(\"Just print the best sol: [\", end=\"\")\n    for var in m.getVars():\n        print(f\"{var.X}\", end=\",\")\n    print(\"]\")\nelse:\n    print(\"No optimal solution found\")\nHowever, when LLMs are used to generate such code snippets, whether through multi‑sample generation or RLVR training, each optimization task typically requires sampling multiple responses in parallel, with each response containing a candidate script (n‑rollouts, e.g., \\(n=8\\) or \\(n=16\\)). Due to the inherent stochasticity of LLMs, these scripts diverge wildly in how they format logs, name variables, and print optimization results, making automated extraction and parsing notoriously brittle.\n\nIncomplete execution log: the code fails to print any information about the optimal value or decision variables (e.g., the script runs without errors but produces no output).\n\nimport gurobipy as gp\nfrom gurobipy import GRB\n\nm = gp.Model()\nx = m.addVar(vtype=GRB.INTEGER, name=\"x\")\ny = m.addVar(vtype=GRB.INTEGER, name=\"y\")\nm.setObjective(x + y, GRB.MINIMIZE)\nm.addConstr(10*x + 6*y &gt;= 200)\nm.addConstr(1000*x + 700*y &lt;= 22000)\nm.addConstr(x &lt;= y)\nm.optimize()\n\nPartial execution log: the code produces incomplete output, printing only the objective function value (optimal value) while omitting the solution vector:\n\nimport gurobipy as gp\nfrom gurobipy import GRB\n\nm = gp.Model()\nx = m.addVar(vtype=GRB.INTEGER, name=\"x\")\ny = m.addVar(vtype=GRB.INTEGER, name=\"y\")\nm.setObjective(x + y, GRB.MINIMIZE)\nm.addConstr(10*x + 6*y &gt;= 200)\nm.addConstr(1000*x + 700*y &lt;= 22000)\nm.addConstr(x &lt;= y)\nm.optimize()\nif m.status == GRB.OPTIMAL:\n    print(f\"The optimal value = {m.objVal}\")\nThis output inconsistency poses a serious challenge for automated evaluation and reward calculation in RLVR training loops. To address it, the authors of SIRL introduced a lightweight Intermediate Representation (IR) Injection framework specifically designed for solver‑integrated optimization training.\nThe core idea is straightforward: rather than relying on prompt engineering to force the LLM to output standardized print statements, the framework dynamically injects a standardized execution block for the generated code snippet. The core idea is simple: instead of relying on prompt engineering to force the LLM to emit standardized print statements, the framework dynamically injects a standardized execution block into the generated code. This augments the execution without imposing rigid formatting constraints on the model’s prompt or text generation. Specifically, the framework’s workflow consists of three key steps:\n\nDynamic instance Matching: Uses regular expressions to precisely capture the model object instance within the generated code. For example, based on standard solver syntax, the regex ()(+).(solve|optimize)() extracts the model instance name (e.g., \\(m\\)), and the specific call method.\nAdaptive status‑check generation – Based on the target solver backend, the framework automatically generates the appropriate optimality check:\n\nCOPT solver → COPT.OPTIMAL\nGurobi solver → GRB.OPTIMAL\n\nForced injection / replacement – Once the model instance and solver action are identified, the harness automatically appends an additional code snippet that injects status verification and standardized output. This ensures the program reliably prints the Status, the Best Obj (optimal objective value), and the Best Sol (all decision variables).\n\nConsider the earlier incomplete Gurobi script, which lacked any output logic. When the injection framework detects m.optimize(), it automatically injects the output logic needed for downstream RLVR—printing the optimal objective value and the full solution vector. The transformation is shown below:\n... # initialization and modeling omitted\nm.optimize()\nAfter injection (modified in‑memory before sandbox execution):\n# ... # initialization and modeling omitted\nm.optimize()\nif m.status == GRB.OPTIMAL:\n    print(f\"Just print the best obj: {m.objVal}\")\n    print(\"Just print the best sol: [\", end=\"\")\n    for var in m.getVars():\n        print(f\"{var.X}\", end=\",\")\n    print(\"]\")\nelse:\n    print(\"No optimal solution found\")\nThis rewrite-and-inject strategy cleanly decouples LLM generation from sandbox execution. By freeing the model from managing complex mathematical modeling and rigid output formatting simultaneously, IR Injection significantly enhances training efficiency, accelerates convergence, and improves evaluation robustness. As detailed in the original paper, the SIRL framework treats external solvers as verifiers within the RLVR paradigm. The pipeline proceeds in three steps: (1) the LLM generates a reasoning trace, a problem formulation, and executable solver code; (2) an IR injection engine intercepts and rewrites the generated scripts; (3) the engine then uses both the runtime execution logs and the resulting .lp model files to compute the final reward. The overall process is illustrated below:\n\n\n\n\n\n\nFigure 1: SIRL RLVR diagram\n\n\n\n\nUnlocking Richer Feedback Signals: From Final results to Internal States\nBy leveraging the Intermediate Representation (IR) Injection mechanism, the SIRL harness reliably standardizes solver outputs, effectively mitigating the variance inherent in LLM-generated formatting. Crucially, the utility of this harness extends well beyond parsing terminal results. By penetrating deeper into the solver’s execution stack, it exposes a wealth of metadata, intermediate search trajectories, and diagnostic telemetry to upper-layer RLVR pipelines and multi-agent frameworks.\nFor complex industrial optimization applications, this deep observability is realized by dynamically embedding lightweight diagnostic hooks throughout the solver’s lifecycle. Rather than relying on sparse, binary success/failure signals, these hooks stream continuous data regarding intermediate optimization states, algorithmic bottlenecks, and structural mathematical traits. Equipped with this high-resolution feedback, LLMs gain the contextual depth necessary for multi-step reflection, precise root-cause diagnosis, and adaptive algorithmic tuning. Consequently, the model transitions from merely observing that a solution failed, to fundamentally understanding why a specific formulation or computational strategy succeeds, stalls, or collapses. Below, we detail representative diagnostic signals extractable via this framework. Here, We outline several representative diagnostic signals extracted via this framework below:\n\nProfiling Execution Quality and Computational Latency:\n\n# Capture solver runtime to benchmark the complexity of the generated model\nsolve_time = m.Runtime\n\n# Extract the complete constraint matrix A (returns scipy.sparse.csr_matrix)\n# This allows us to detect if the LLM's formulation inadvertently caused a highly dense or ill-conditioned matrix\nA = m.getA()\nHere we retrieve the constraint matrix A as a scipy.sparse.csr_matrix, which iis Useful for evaluating sparse structure density and condition metrics.\n\nRoot Cause Analysis (RCA) for Infeasibility:\n\nif m.Status == GRB.INFEASIBLE:\n    # Compute the Irreducible Inconsistent Subsystem (IIS)\n    m.computeIIS()\n    # Precisely isolate the fragment of conflicting constraints for the LLM to debug\n    m.write(\"conflict_model.ilp\")\nWhen an LLM produces contradictory constraints, runtime error messages are often insufficient. We can instruct the solver to compute an Irreducible Inconsistent Subsystem (IIS) to isolate the conflict.\n\nProbe C: Economic Signals and Sensitivity Analysis:\n\n# Get shadow prices for all constraints\npi_vals = m.getAttr('Pi', m.getConstrs())\n# Get reduced costs for all decision variables\nrc_vals = m.getAttr('RC', m.getVars())\nSensitivity Analysis & Economic SignalsFor linear programming tasks, dual metrics provide direct operational signals beyond primal decision variable value. By pairing solver backends with an IR-injection Harness engine, we establish a robust interface between probabilistic language models and deterministic mathematical solvers—enabling scalable RL training and dependable real-world deployments."
  },
  {
    "objectID": "posts/02_optharness.html#toward-multi-agent-collaboration-generalizing-the-harness-framework",
    "href": "posts/02_optharness.html#toward-multi-agent-collaboration-generalizing-the-harness-framework",
    "title": "Harness Engineering for Optimization Modeling: Strategy, Implementation, and Expansion",
    "section": "Toward Multi-Agent Collaboration: Generalizing the Harness Framework",
    "text": "Toward Multi-Agent Collaboration: Generalizing the Harness Framework\n\nA General Multi-Agent Framework\nBuilding upon our aforementioned Intermediate Representation (IR) Injection mechanism, the Harness framework’s capacity for optimization modeling can naturally extends to higher-level optimization task solving and can serve as the perfect foundation for a Multi-Agent System (MAS) tailored to to tackle complex, enterprise-grade operations research problems.\nInspired by multi-agent optimization frameworks such as Optimus*(AhmadiTeshnizi et al. 2023), we can we can construct a fully automated workflow designed for high-complexity optimization tasks. built around a clear pipeline: Problem Parsing \\(\\to\\) Code Generation \\(\\to\\) Execution with Feedback \\(\\to\\) Diagnostic Remediation.\n[ Problem Description ]\n          |\n          v\n+------------------+\n|  Agent A: Modeler|  (Extracts business domain boundaries & math objectives)\n+--------+---------+\n         |\n         v\n+------------------+\n| Agent B: Solver  |  (Generates code + injects diagnostic hooks: IIS, duals)\n+--------+---------+\n         |\n         v\n+------------------+\n| Harness Engine   |  (Rewrite and Executes in sandbox, extracts logs & metadata)\n+--------+---------+\n         |\n         v\n+------------------+\n|Agent C: Reflector|  (Diagnoses matrix ill-conditioning, IIS conflicts, dual gaps)\n+--------+---------+\n         |\n         v\n+------------------+\n| Agent D: Refiner |  (Relaxes constraints, adjusts bounds, resolves logic issues)\n+------------------+\n\n\nAgent A (Business Modeler): Parses unstructured natural language to extract core business objectives, operational constraints, and domain assumptions.\nAgent B (Optimization Solver): Translates specifications into code while leveraging the Harness to inject low-level diagnostic hooks (e.g., model.computeIIS(), dual variable extractors) for downstream observability.\nAgent C (Reflection Expert): Analyzes execution logs, IIS output, and shadow prices returned from the sandbox, identifying underlying issues like numerical instability, contradictory constraints, or over-constrained formulations.\nAgent D (Code Refiner): Applies targeted repairs based on Agent C’s diagnostics—relaxing tight bounds, updating penalty factors, or resolving logical conflicts—to complete the iteration cycle/loop.\n\nThe key role of the Harness is to make the solver’s otherwise hidden internal states explicit, structured, and machine-readable. Through structured code injection and solver instrumentation, information such as execution traces, IIS results, dual variables, and performance statistics becomes directly accessible to the agents.\nThis dramatically changes the optimization loop. Instead of relying on a coarse success/failure signal, agents can reason over detailed solver evidence and perform targeted revisions. The Harness therefore acts as an information engine that connects low-level solver execution with high-level agent reasoning.\nAs LLM-based optimization modeling moves toward real industrial workloads, such an architecture becomes increasingly valuable. The same closed-loop design can be adapted to supply-chain planning, energy systems, portfolio optimization, transportation, manufacturing, and other domains where solving the mathematical program is only one component of the overall decision process.\n\n\n\n\n\n\n\n\nApplication\nSolver Signal Exposed by the Harness\nAdaptive Agent Action\n\n\n\n\nPower-System Unit Commitment\nDual information associated with operational or network constraints\nIdentify economically stressed parts of the system and revise generation, reserve, or dispatch decisions accordingly\n\n\nSupply-Chain Network Optimization\nIIS / conflicting constraint subsets\nLocate contradictory inventory, capacity, or service-level constraints and selectively convert hard requirements into penalized soft constraints\n\n\nPortfolio Optimization\nShadow prices of capital, exposure, or risk constraints\nEstimate the marginal value of relaxing investment restrictions and adjust allocation limits across asset groups\n\n\n\nIn this pipeline, the solver is no longer merely the execution backend of an LLM-generated program. It becomes an active participant in the reasoning loop, continuously producing structured signals that specialized agents can interpret, diagnose, and act upon.\n\n\nHarness Engineering for Power-System Optimization\nTo illustrate how our Harness-driven multi-agent architecture scales to complex vertical domains, we examine ProOPF*Shen et al. (2026) as a concrete case study. Power system optimization presents a uniquely demanding environment: tasks are strictly time-critical, tightly bound by physical laws, and historically dependent on the tacit, manual expertise of power engineers. With the powerfulness of semantic reasoning and formal code generation, LLMs agents can translate an power operator’s high-lvel requirements directly into executable matehmatical modeling and solver code paving a practical path toward end-to-end automated power system modeling.\nHowever, existing optimization benchmarks mostly target broad, cross-domain tasks and fall short of the strict standards required for industrial power systems. In real-world grid operations, “correct” code means much more than passing a syntax check or reaching a feasible status. Mission-critical workflows like Optimal Power Flow (OPF) must satisfy complex physical laws, operational bounds, and network topology constraints. ProOPF addresses this limitation by offering a specialized benchmark for professional-grade power system optimization. It provides a realistic testbed to measure whether LLMs can accurately translate intricate grid specifications into production-ready, executable optimization code. Specifically, ProOPF encapsulates a spectrum of benchmark power system optimization problems—including OPF, SCUC, and SCED—characterized by deeply nested constraint structures. Formulations in this domain must concurrently satisfy physical power-flow equations, discrete equipment status variables, hard operating limits, \\(N-1\\) security criteria, and inter-temporal scheduling dynamics. This multi-faceted modeling friction positions ProOPF as an ideal benchmark for validating the proposed Harness framework\nExamining the mathematical taxonomy of these tasks demonstrates why standard LLM code generation falls short. The solver ecosystem for power grids is fundamentally heterogeneous. Rather than relying on a uniform compilation target, an intelligent LLM agent must dynamically route formulations based on their mathematical geometry and required execution loop:\n\n\n\n\n\n\n\n\nGrid Problem\nMathematical Formulation\nTypical Solver / Execution Pathway\n\n\n\n\nAC-OPF\nNon-convex NLP\nMATPOWER MIPS / IPOPT / KNITRO\n\n\nDC-OPF & SCED\nLP / QP\nGurobi / COPT / CPLEX\n\n\nSCUC\nMILP\nBranch-and-Bound + Cutting Planes\n\n\nSCOPF (\\(N-1\\))\nSecurity-Constrained Extension\nIterative Contingency Screening + Master Problem Re-solving\n\n\n\nIn this domain, both human experts and LLM-driven agents confront a fundamental bottleneck: superficial algorithmic convergence. Even when the generated code is syntactically pristine and the solver achieves optimality, the underlying formulation may entirely lack engineering fidelity. Subtle errors—such as topological index misalignments, inconsistent load units, the omission of critical \\(N-1\\) security scenarios, or redundant reserve constraints—are insidious because they fail silently. Rather than throwing explicit exceptions, they quietly distort the feasible region, yielding solutions that are mathematically optimal yet physically invalid.To counter this, our IR Injection harness framework fundamentally alters the evaluation paradigm. By employing a “rewrite-and-inject” methodology, we elevate the modeling and optimization process from a black box into a fully executable, diagnosable, and replayable trajectory. This injection mechanism allows us to harvest a series of structured, intermediate execution byproducts. It is crucial to emphasize that the exact type of diagnostic byproduct extracted is inherently coupled to the mathematical structure of the problem:\n\nLP / MILP Stack (DC-OPF, SCED, SCUC): Exports standardized .lp and .mps snapshots for structural constraint diffing; generates conflict.ilp to pinpoint constraint contradictions; parses solver.log for presolve reduction ratios, root-node relaxations, and MIP gaps.\nNon-Convex NLP Stack (AC-OPF): Tracks KKT first-order optimality residuals, complementarity slackness violations, non-linear convergence trajectories, exit flags (e.g., local optima vs. unbounded step-length pull-backs), and feasibility recovery distributions.\nShared Output Layer: Emits a normalized solution.json containing primal-dual solution vectors, operational constraint violation tables (e.g., branch MVA flows against RATE_A, bus voltage bounds \\([V_{\\min}, V_{\\max}]\\)), and runtime telemetry.\n\nWe engineered a streamlined multi-agent system where distinct agents handle generation, verification, error-correction, and policy switching. The key innovation lies in using our harness framework to pipe structured intermediate data across the system, establishing a single source of ‘shared facts’ for seamless agent collaboration. The benchmark results speak for themselves: in intricate power grid optimization tasks, our framework massively eclipses current closed-source SOTA models, proving the robustness and transformative potential of the Harness framework in real-world power optimization."
  },
  {
    "objectID": "posts/02_optharness.html#conclusion",
    "href": "posts/02_optharness.html#conclusion",
    "title": "Harness Engineering for Optimization Modeling: Strategy, Implementation, and Expansion",
    "section": "Conclusion",
    "text": "Conclusion\nIn this article, we detailed the foundational Harness framework underlying the SIRL paper. SIRL stands as a pioneering effort in applying Reinforcement Learning with Verifiable Rewards (RLVR) to the domain of optimization modeling. Tailored specifically for the idiosyncrasies of optimization formulation and solver syntax, this Harness strategy forms the indispensable engineering backbone of SIRL’s RLVR pipeline. It supplies a robust execution environment and a dense stream of verifiable reward signals, directly empowering the model’s advanced reasoning and decision-making.\nConcurrently, we explored the generalization of this Harness framework to broader multi-agent synergistic systems and specialized vertical domains, such as power system optimization, projecting its utility for next-generation optimization agents. As optimization modeling penetrates deeper into real-world industrial applications, this architecture will assume an increasingly critical role. Notably, this Harness infrastructure was conceptualized and matured long before “Harness engineering” emerged as a mainstream AI trend, demonstrating a precise grasp of the core engineering prerequisites for automated mathematical modeling.\nWe invite interested researchers and industry practitioners to review our open-source code and model weights via GitHub. For technical discussions or inquiries, please leave a comment or contact the team with the email chenyitian@tokentide.cn."
  }
]